/**
 * Tier 1 · project · "Reading the results, and the write-up".
 *
 * The second half of the Tier 1 project, split from `tier1-project` under the
 * zero-to-hero contract
 * (docs/superpowers/specs/2026-09-21-course-readability-design.md). The first
 * half is every number the learner predicts with a pencil; this one is every
 * number the run gives back, the two mechanisms that explain most of the gap
 * between them, the residual that stays unexplained, and the rubric a good
 * write-up is marked against.
 *
 * It loads `tier1-project`'s own scene and its three variants — the same
 * builder, the same array — so the two ids replay to the same recorded
 * timeline hashes and the split costs the reader nothing.
 *
 * Every measurement quoted below comes from a 10 s run in
 * tests/course/tier1-project-review.test.ts; every prediction it is compared
 * against is recomputed there from the engine's own functions.
 */
import { type Lesson } from '../lessonKit'
import { projectFlat, projectJumps, projectVariants } from './tier1-project'

export const tier1ProjectReview: Lesson = {
  id: 'tier1-project-review',
  module: 6,
  title: '项目——读懂结果',
  why: '现在可以打开仿真了。你的四个预测有的会命中，有的不会；而没中的那些，比中了的更值钱。这后半程把计划和实跑并排摆开，点名造成大部分差距的两个机制，并用仿真器自己的单位把它们各自定量。剩下的那一部分，如实报出来，而不是辩过去——这恰恰是这次练习里能带到真实测量中去的那一半。',
  outcomes: [
    '把四次实跑与四个预测并排摆开，说出每一个是在哪里失手的',
    '把你心里那个量，和屏幕真正在数的那个量区分开',
    '点名一个机制，用模型自己的单位给它定量，并如实报出剩下的部分',
    '把项目写成别人能逐行核对的样子',
  ],
  needs: ['tier1-project', 'hidden', 'anomaly'],
  terms: [
    { term: 'estimator', plain: '你在屏幕上真正数的那样东西，用来代替你心里想要的那个量' },
  ],
  picture: [
    { heading: '四次实跑，一张纸', text: '载入这户人家，跑一遍，再把三个变体各跑一遍，把每个结果写到纸上它对应的那个预测旁边。先别急着改任何东西。四个量里有两个几乎分毫不差地回来了；另外两个差得足够多，多到必须指名道姓地说出计划里哪一处错了。' },
    { kind: 'watch', jump: 1, heading: '看第一次碰撞', text: '跳到第一次碰撞，然后在第一毫秒里往回步进。两台笔记本一起起跑，短帧远早于长帧结束；接着发短帧的那台站点（STA）又回来了——踩在一个还在进行中的帧上。' },
    { heading: '“一致”不一定是一致', text: '你算出的碰撞数，和屏幕上的重传（retry）数，看上去近得可以握手言和。但它们不是同一件事。重传数的是“丢掉的帧”，重叠数的是“撞在一起的两个帧”。在一户两台笔记本到达强度相差悬殊的房子里，较强的那一帧照样被解了出来——这就是捕获——于是屏幕数到的损失，远少于实际发生的相遇。把你正在数的那样东西点名——这就是估计量——然后再去比较。' },
    { heading: '一个只在一个方向上失效的假设', text: '计划假设两台笔记本彼此听得见。它们确实听得见——很微弱，而且只在它们正在听的时候。如果对方的前导码（preamble）到达时本机正在发送，它就永远抓不到那个前导码；此后剩下的只有残余能量，而这点能量远不足以让空闲信道评估（CCA）保持置忙。于是它熬完等待、数完倒数，又发了出去——正好压在一个它已经听不见的帧上。' },
    { heading: '一次事件，三个时钟', text: '同一个瞬间，屋里每个人给它的定价都不一样。回答没等到的那台，要熬完自己的 ACK 超时（ACK timeout）。锁上了前导码却解不出这一帧的那台，必须假定有一个它听不见的回答正在路上，于是要等更长的扩展帧间间隔（extended interframe space, EIFS）。而什么也没听见的那台，等一个普通的分布式帧间间隔（DCF interframe space, DIFS）就接着走。' },
    { heading: '还差的那一点，以及写报告', text: '两个点了名的机制解释了大部分差距，而剩下的大部分要记在速率控制头上：屏幕上每个帧都比你定价的那个略长，因为接连失败的发送方会往下降档，而它分不清碰撞与衰落。把这些都算完，还剩下两个百分点左右。把它们当作残差报出来。说出这一点是答案的一部分，而不是答案的缺陷。' },
  ],
  numbers: [
    { kind: 'table', heading: '预测对实测：这户人家的十秒钟', head: [
      '量', '预测', '实测',
    ], rows: [
      ['等级', 'MCS 13 / MCS 2', 'MCS 13 / MCS 2'],
      ['数据帧', '129.6 / 524.0 µs',
        '全程均值 148.1 / 600.9 µs'],
      ['碰撞概率', '10.46 %',
        '23.15 % 的尝试重叠；12.59 % 以重传收场'],
      ['合计交付', '25.585 Mb/s', '22.610 Mb/s'],
      ['各自帧数', '相同', '9,719 / 9,123'],
      ['空口占比', '19.8 % / 80.2 %', '21.2 % / 78.8 %'],
      ['书房笔记本独占', '43.621 Mb/s', '42.470 Mb/s'],
    ] },
    { kind: 'table', heading: '三个变体，跑出来的结果', head: [
      '变体', '预测', '实测', '成因',
    ], rows: [
      ['书房笔记本搬到客厅', '19.350 Mb/s · 10.46 %',
        '16.796 = 8.737 + 8.059 Mb/s · 11.10 %',
        '两个帧都长且相近：迟到起跑几乎消失'],
      ['多一台平板', '29.574 Mb/s · 17.81 %',
        '21.184 = 7.734 + 5.852 + 7.597 Mb/s · 17.13 %',
        '少了 28 %：客厅笔记本被压到调制与编码方式（MCS）0'],
      ['客厅笔记本离线', '43.621 Mb/s', '42.470 Mb/s · 0.28 %',
        '没有人可撞'],
    ] },
    { kind: 'table', heading: '差距出在哪里', head: [
      '机制', '日志里看到的', '有多大',
    ], rows: [
      ['捕获把输家救了回来', '4,990 次重叠 → 2,713 次重传',
        '约 46 % 的输家活了下来；两台相差 34 dB'],
      ['聋掉的迟到起跑', '2,399 次笔记本互撞中有 1,342 次',
        '10.46 % → 23.15 %'],
      ['帧在速率控制下变长', '148.1 vs 129.6 µs · 600.9 vs 524.0 µs',
        '吞吐少掉的 11.6 % 的大部分'],
      ['残差', '日志里没有东西指认它',
        '两个百分点左右，如实报出而不拟合掉'],
    ] },
    { heading: '两台笔记本互相听到的电平', text: '它们到达对方处的电平是 −72.64 dBm：高于检出一个前导码所需的 −82 dBm，又远低于仅凭能量判忙所需的 −62 dBm。抓到了前导码就冻结；错过了，信道读出来就是空闲。' },
    { kind: 'table', heading: '同一帧之后，各自要等多久', head: [
      '这台站点', '它听到了什么', '它要等的',
    ], rows: [
      ['碰撞的一方', '回答没有来', '45 µs'],
      ['客厅笔记本', '21.35 dB，而这一帧要求 44.99 dB', 'EIFS，94 µs——共 9,499 次'],
      ['什么也没听见的', '完全没有前导码', 'DIFS, 34 µs'],
    ] },
    { kind: 'steps', heading: '怎么批一张答题纸', items: [
      '先批（a）。两个到达电平都应落在 −40.73 与 −75.15 dBm 的一个分贝之内，而每个等级都要用“要求加余量”说清。等级就写在每台笔记本的第一个数据帧（data frame）上。',
      '（b）就拿同两个帧来批：屏幕上是 129.6 与 524.0 µs。这里答错的人，通常是漏掉了帧头（MAC header）与校验字节，或者把符号（symbol）数向下取整而不是向上。',
      '（c）先问用的是哪个估计量。把实测的重传率和预测的碰撞概率当成“对上了”的答案，根本没有把同一件事比两遍。',
      '（d）看时长，不看轮次：两台笔记本发出的帧数差不多，占空口却是 21.2% 与 78.8%。把空口平均分的答案，就是错的。',
      '差距放到最后批。两个机制，都要点名、都要用模型自己的单位定量、且都要指得出记录：捕获看重叠数对重传数，“聋掉的迟到起跑”看碰撞发生的时刻。',
      '最后找残差。一张最后写着“还剩两个百分点没能解释”的答题纸，比一张靠调常数让两列重合的答题纸分数更高。',
    ] },
    { kind: 'table', heading: '好答案长什么样', head: [
      '部分', '好答案',
    ], rows: [
      ['(a)', '两个电平误差都在一分贝内，墙只数一次，等级用“要求加余量”说清'],
      ['(b)', '符号数向上取整，算进头部与校验字节，回答的速率是推出来的而不是想当然的'],
      ['(c)', '给出碰撞概率时不带速率，并说清用的是哪个估计量'],
      ['(d)', '帧数相同而空口时间（airtime）不同，占比由帧时长算出'],
      ['差距', '点名两个机制，各自用模型自己的单位定量，并报出残差'],
    ] },
  ],
  deeper: [
    { heading: '为什么两个误差差点抵消', text: '重传率看上去与预测的碰撞概率很接近，而其中的原因应当令人不安：两个符号相反的误差。真实的重叠率是预测的两倍多，那是“聋掉的迟到起跑”造成的；捕获随后又把其中约 46 % 从重传计数器前面藏了起来。两者一乘，就落回到模型附近——可模型的两半，没有一半是对的。' },
    { heading: '为什么搬走之后的变体吻合得那么好', text: '把书房笔记本挪到同一堵墙之后，两个帧都变得又长又几乎相等。一台发完 415 µs 的帧、又等完超时的站点，不再会发现对方仍在发，于是“聋掉的迟到起跑”几乎无从下手——实测的重叠率也就落在模型一个百分点之内。吞吐依旧偏低，因为速率控制完全不受这些影响。' },
  ],
  sources: [
    '本课倚重的两条载波侦听门限——检出前导码的 −82 dBm 与仅凭能量的 −62 dBm——是本仿真器的 CCA 模型，取材自 IEEE Std 802.11-2024 §17.3.10.6 的灵敏度要求，而非照抄。',
    '接收失败之后的 EIFS，以及“不得把这样的介质径直当作空闲”这条规则，见 §10.3.2.3；ACK 超时见 §10.3.2.9。',
    '上面每一个计数、比值和时刻都是本仿真器的取值，靠场景自己的种子即可复现：对项目那户人家及其三个变体各跑十秒。其背后的捕获规则与速率控制算法都是模型取值，而残差正是它们解释不掉的那一部分。',
  ],
  scenario: () => projectFlat(),
  variants: projectVariants,
  jumps: projectJumps,
  observe: [
    '第一毫秒里：两台笔记本同时起跑，书房笔记本 129.6 µs 的色块结束，然后在 406.6 µs 又发了一次——落在客厅笔记本那个仍在进行的 524.0 µs 帧里。',
    '在书房笔记本发完一帧之后，悬停客厅笔记本的等待色块：上面写的是 94 µs，而不是 34。它锁上了一个解不出来的前导码，只好为一个它听不见的回答留出时间。',
    '把两条绿色泳道并排读：色块数量相当，成功交付 9,719 对 9,123，但客厅那条泳道被填满的时间大约是四倍。轮次相同、空口不同，一屏之内看尽。',
  ],
  tryThis: [
    '先跑加平板的变体，再跑空房子的那个。两次的碰撞概率都几乎与预测分毫不差，吞吐却不是。写下你的四个预测里，哪些经得起换场景，哪些经不起。',
    '把项目写成一份报告：四个预测、四个读数、两个点了名并用模型自己单位定了量的机制，以及如实写出的残差。读它的人应当能针对某一行具体地反驳你。',
  ],
  quiz: [
    {
      q: '你预测的碰撞概率是 10.46 %，实跑中 12.59 % 的尝试发生了重传。为什么此刻宣告一致是个错误？',
      options: [
        '12.59 % 超出了模型应有的容差',
        '这个估计量测的是别的东西：实际有 23.15 % 的尝试重叠，而捕获救回了约 46 % 的输家',
        '十秒的样本太短',
      ],
      answer: 1,
      explain: '重传数的是丢失的帧，而不是重叠；两台笔记本之间差着 34 dB，这就是两件不同的事。十秒已经是数千次尝试，而模型本身也没有什么容差带。',
    },
    {
      q: '客厅笔记本的帧还在空中时，书房笔记本又发了一次。是哪条规则允许的？',
      options: [
        '网络分配向量（NAV）已经到期，所以信道在形式上是空闲的',
        '那个前导码在本机发送期间到达而被错过，于是只剩能量可判——而 −72.64 dBm 低于 −62 dBm',
        '站点在 ACK 超时加一个 DIFS 之后总是可以开始发送',
      ],
      answer: 1,
      explain: '−82 dBm 的门限只适用于电台确实检测到了的前导码；其余一切只算能量，而能量必须达到 −62 dBm 才能让 CCA 置忙。两个帧都没有给对方留下可读的 NAV。',
    },
  ],
}
