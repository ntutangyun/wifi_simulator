/**
 * Wi-Fi Tier 1 · M2 · Channel access · NAV — reserving with a promise.
 *
 * Rewritten to the zero-to-hero contract
 * (docs/superpowers/specs/2026-09-21-course-readability-design.md): what
 * listening cannot tell you, the announcement every header carries, and only
 * then the long frozen wait taken apart µs by µs. The "what A knows, and when"
 * material moved to `deeper`.
 *
 * Every number quoted below is pinned in tests/course/nav.test.ts. The
 * scenario builder is unchanged, so the recorded timeline hash stays identical.
 */
import { type Lesson, oneRoom, node, sc, firstNav, J } from '../lessonKit'

export const nav: Lesson = {
  id: 'nav',
  module: 1,
  title: 'NAV——用“预告”预约信道',
  why: '光靠听，站点（STA）只知道空口此刻忙不忙。可一场对话里是有缝的——回答回来之前那一小段停顿；而回答本身，还可能来自一台远得根本听不见的设备。只相信自己耳朵的站点，这两处都会一头撞进去。于是每一帧都会预告本次交互还要多久，听到的人则改为在心里挂一个倒计时。',
  outcomes: [
    '说清站点往自己的计时器里装的是什么、从哪些帧里装',
    '解释为什么光听信道保护不了一次交互',
    '从数据帧上读出那份预约，并拿它保护的那个回答来核对',
  ],
  needs: ['backoff'],
  terms: [
    { term: 'Duration', plain: '帧头靠前的一个字段，说明这一帧之后，本次交互还需要多久' },
    { term: 'NAV', plain: '网络分配向量：站点自己挂着的一个倒计时，只要它还在走，哪怕一片寂静也当信道是忙的' },
    { term: 'virtual carrier sense', plain: '把信道当作忙，依据的是别人告诉你的话，而不是你听得见的东西' },
  ],
  picture: [
    { heading: '耳朵告诉不了你的事', text: '侦听信道只回答一个问题：此刻空口上有没有能量？帧还在发的时候，这就够了。可帧发完之后那段停顿里就不够了——那时空口确实是空的，交互却还没完。而当下一帧将由房间另一头、你压根听不见的设备发出时，它同样不够。' },
    { heading: '每个帧头里的一句承诺', text: '所以每一帧靠前的位置都带着一个小字段——Duration——说明这一帧结束之后，本次交互还需要多少时间。凡是解出这一帧的人，都会把这个数拿去起一个倒计时，也就是 NAV，包括那些根本不是收件人的站点。只要倒计时还在走，站点的行为就完全等同于“信道忙”，不管听上去有多安静。' },
    { kind: 'watch', jump: 0, heading: '看一次计时器被装上', text: '载入仿真，跳到第一次预约被装上的地方。那是旁听者，它在对一帧与自己毫无关系的数据帧作出反应：帧一结束，它的倒计时就在那一刻起跑。' },
    { heading: '因为被告知，所以算忙', text: '这就是虚拟载波侦听，是这条规则里与射频毫无关系的那一半。真正的侦听保护的是还在空中的那一帧；而那句预告保护的，是它之后的一切。有一句话值得记住：耳朵守的是帧，承诺守的是间隙和回答。' },
    { heading: '只算剩下的部分', text: '注意这个数没把什么算进去：它自己所在的那一帧。这本来也没必要——那一帧还在空中时，所有人的耳朵本来就报“忙”。所以数据帧预告的，只是那段停顿加上那个回答；而同一次交互里越靠后的帧，预告的剩余时长就越短。' },
    { heading: '冻住了，却不知道要冻多久', text: '一个正数着自己那份等待的站点，在有帧开始时就地停住，而那一刻它根本不知道这次打断会持续多久。它从帧头里得知这一帧有多长；只有当整帧都被解了出来——从头到尾，比值都够它所用那一级速率的要求——它才可以相信那句预告，并装上自己的倒计时。这段等待的大部分时间，它都不知道等待何时结束。' },
  ],
  numbers: [
    { kind: 'table', heading: '44 µs 是怎么来的', head: [
      '组成', '时长', '出处',
    ], rows: [
      ['回答之前的停顿', '16 µs', '§17.4.4'],
      ['回答本身：14 个字节，速率按“空口时间”第 5 步那条控制回应规则定，这里是 24 Mb/s', '28 µs', '§17.4.3'],
      ['数据帧预告的量', '44 µs', '§9.2.4.2'],
      ['回答预告的量', '0 µs', '交互到此结束'],
    ] },
    { heading: '每一次都是同一个数', text: '本轮仿真里 576 个数据帧预告的都是同一个 44 µs，而旁听者的倒计时，恰好在回答结束的那一纳秒到期——513 次无一例外，不早也不晚。' },
    { kind: 'table', heading: '把一段长长的冻结拆开', head: [
      '组成', 'µs', 'A 在熬的是什么',
    ], rows: [
      ['B 那一帧的剩余部分', '248', '信道听得出是忙的'],
      ['B 预告的那份预约', '44', '虽然安静，但已被预订'],
      ['一个货真价实的 DIFS', '34', '真正的空闲'],
      ['合计', '326',
       '之后 A 的退避计数从 3 继续——那是空口变忙时它还欠着的空闲时隙数'],
    ] },
    { heading: 'A 什么时候才算得出总数', text: 'A 在 498 µs 冻结，当时它只知道信道忙。B 的帧在 746 µs 结束，并且被解了出来；直到这时那份预约才可信，它一直管到 790 µs，最后那段等待再把 A 送到 824 µs。' },
    { kind: 'steps', heading: '从帧头里的一个字段，到一台冻住的站点', items: [
      '发送方在发出去之前先算清楚：这一帧结束之后还剩些什么——一段停顿加上回答的空口时间，这里是 16 + 28 = 44 µs——再把这个数写进帧头的 Duration 字段。',
      '结束一次交互的那一帧，预告的是“后面没有了”。确认帧带的就是 0 µs；而写着零的 Duration，谁的计时器也装不上。',
      '一台把这一帧整个解了出来、再发现收件人是别人的站点，会算出一个时刻：这一帧结束的那一刻，加上它预告的 Duration。',
      '只有当这个时刻比它手里已经挂着的那个倒计时更晚时，它才采用。一帧可以把一份预约往后推，却永远不能把它往回拉。',
      '装上这个倒计时之后发生的事，和侦听到能量时一模一样：正在走的退避计数在当前那个值上冻住，正在走的那段间隙作废，这次尝试被记上“推迟过”。',
      '从这一刻起，直到它装上的那个时刻为止，站点对介质的判定一律是“忙”，不管耳朵报的是什么。忙 = 侦听为忙，或者倒计时尚未到期——两者成立一个就够了。',
      '到了那个时刻，倒计时解除。如果这时侦听报的是空闲，站点就开始走它那段间隙，并从冻住的那个值恢复接入。预约还有两种提前解除的办法，留到后面的课。',
    ] },
  ],
  deeper: [
    { heading: '站点手里从来没有时刻表', text: '326 µs 这个总数，最早要到 746 µs 才算得出来，而那时等待本身已经过去了 248 µs，约四分之三。就算算出来了，这个数也是有条件的：最后那 34 µs 里若又有一帧开始，它只会继续变长。站点手里没有任何时刻表，只有一个不断修正的信念——“我最早可能在 ___ 恢复”——每来一个事件就重算一次；穿过这团迷雾时，它随身带着的只有一个数字：冻结住的那个计数。' },
    { heading: '为什么标签写的是一回事，色块代表的是另一回事', text: '泳道把这整整 326 µs 画成一个色块，标签写的却是最后那一味组成——A 正在“等”的那个东西——而不是三段之和。它并不是一段 326 µs 的那种等待；它是 A 在自己的倒数获准恢复之前必须熬过的全部，而标签只点了三段里的最后一段。' },
  ],
  sources: [
    'IEEE Std 802.11-2024 的 §10.3.2.4 给出 NAV 的更新规则：站点正确收到一帧不是发给自己的帧时，把 NAV 设为“当前值”与“帧结束时刻加 Duration”中较晚的那个。',
    'Duration 字段本身，以及“它是从当前帧结束起算、以微秒为单位的一段时间”这一含义，见 §9.2.4.2。',
    '248 µs 的帧、28 µs 的确认帧，以及上面引用的每一个时刻，都是本仿真器的场景，靠随机种子即可复现，并非取自标准正文。',
  ],
  scenario: () => sc(oneRoom(), [
    node('ap', 'AP', 'ap', 5, 4, 'eht', 'idle'),
    node('sta-1', 'Talker A', 'sta', 3.5, 5, 'nonht', 'saturated'),
    node('sta-2', 'Talker B', 'sta', 6.5, 5, 'nonht', 'saturated'),
    node('sta-3', 'Listener', 'sta', 5, 6.5, 'nonht', 'browsing'),
  ]),
  jumps: [
    J('第一次设置 NAV', firstNav),
  ],
  observe: [
    '泳道下方那些细细的紫条就是倒计时。它们恰好在回答结束的那一瞬间到期——旁听者的 513 条里随便挑一条核对都一样。',
    '在一帧与它的回答之间的间隙里暂停。空口一片安静，旁听者自己的侦听也报空闲——可它依然不发。',
    '悬停在一个数据块上，读它的 Duration：每一个都是 44 µs，也就是那段停顿加上它正等着的那个回答。',
  ],
  tryThis: [
    '在间隙里暂停，打开旁听者的检视器：载波侦听空闲，倒计时在走。同一瞬间，“信道忙不忙”有两个不同的答案。',
    '用微秒按钮把 A 从 498 µs 走到 824 µs，每走到一处就说出：它此刻熬的是三段等待里的哪一段。',
  ],
  quiz: [
    {
      q: '站点往自己的倒计时里装的到底是什么？',
      options: [
        '它测到的信号强度',
        '任何它正确解出、而且不是发给自己的帧里的 Duration',
        '一个随机的等待时间',
      ],
      answer: 1,
      explain: '这一帧必须先被整个解出来——一句接收端根本没读出来的预告毫无价值。至于是不是收件人，并不要求。',
    },
    {
      q: '数据帧的预告为什么不把这一帧自己算进去？',
      options: [
        '因为发送方不知道自己这一帧有多长',
        '因为这一帧还在空中时，普通的侦听本来就报“忙”',
        '因为那个字段太小，装不下这个数',
      ],
      answer: 1,
      explain: '这句预告存在的意义，正是去盖住侦听盖不住的部分：那段安静的停顿，以及一个可能来自你听不见之处的回答。',
    },
  ],
}
