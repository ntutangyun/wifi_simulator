/**
 * Wi-Fi Tier 1 · M2 · Channel access · Random backoff & collisions.
 *
 * Rewritten to the zero-to-hero contract
 * (docs/superpowers/specs/2026-09-21-course-readability-design.md): why two
 * identical stations need a die at all, what the countdown does, and how a
 * sender that cannot hear a collision finds out about one. The dense material
 * that used to open the lesson — the deadline's three pieces, the buried
 * preambles, the EIFS that is never armed — lives in `numbers` and `deeper`.
 *
 * Every number quoted below is pinned in tests/course/backoff.test.ts. The
 * scenario builder is unchanged, so the recorded timeline hash stays identical.
 */
import { type Lesson, oneRoom, node, sc, firstCollision, firstRetry, firstFreeze, J } from '../lessonKit'

export const backoff: Lesson = {
  id: 'backoff',
  module: 1,
  title: '随机退避与碰撞',
  why: '光是等，解决不了争端。如果两台站点（STA）都憋着、等信道安静下来，那它们会在同一瞬间听到它安静下来，然后一起开口。没有裁判的情况下，Wi-Fi 只能用唯一可行的办法来打破平局：每台站点掷一次骰子，点数小的先说。这一课我们盯着骰子看——也看看两颗骰子点数相同时会发生什么。',
  outcomes: [
    '用自己的话说清“抽一个数、再倒着数下去”这条规则',
    '解释发送方是怎么发现一次它根本听不见的碰撞的',
    '说清把窗口翻倍买到了什么、又付出了什么',
  ],
  needs: ['ifs'],
  terms: [
    { term: 'backoff', plain: '站点在获准开口之前，要倒着数完的那个随机的空闲时隙数' },
    { term: 'CW', plain: '竞争窗口：抽那个随机数时，取值范围的上限' },
    { term: 'ACK timeout', plain: '过了这个期限，发送方就不再指望回答，判这一帧已经丢了' },
  ],
  picture: [
    { heading: '两台站点，同一瞬间', text: '这里的两台站点都很忙，都在等信道，遵守的也是同一条规则。要求的那段安静一走完，它们的状态一模一样——也就是说，一条不带随机性的规则，会让它们在同一微秒开口，每一次都这样，永远这样。讲礼貌是不够的。必须有点什么，让两台一模一样的站点做出不一样的事。' },
    { heading: '先掷骰子，再倒着数', text: '于是每台站点抽一个随机整数，把它当作“要熬过的空闲时隙数”：这就是它的退避值。信道每安静一个时隙，这个数就减一；减到零就发。抽得小的赢，而由于两边各抽各的，赢家每一轮都可能换人。若中途有帧开始，计数就地冻结，之后从停下的那个数继续，谁都不会把已经等过的时间白等。' },
    { kind: 'watch', jump: 0, heading: '去看一次碰撞', text: '载入仿真，跳到第一次碰撞。两帧在同一瞬间开始，彼此叠在一起；在红色刻度处，接入点（AP）报告说这两帧它一个都没锁定。' },
    { heading: '当两颗骰子点数相同', text: '没有任何机制能阻止两台站点抽到同一个数。一旦抽到，两边的计数在同一个时隙同时归零，两帧一起发出去，彼此重叠。两个发送方什么都没察觉：无线电在发送时听不见。它们最先得知的事情，是自己等的那个回答没来。' },
    { heading: '沉默需要一个期限', text: '于是发送方在自己这帧结束的那一刻起表。如果回答真在路上，到这时候早该被察觉了：接收端理应先停的那一小段、再多给一个空闲时隙以防回答开口晚了一点、再加上无线电察觉“有信号开始了”所需的时间。过了这个点，沉默就是判决。ACK 超时到期，这一帧被判丢失，站点必须重来。' },
    { heading: '把窗口翻倍', text: '再拿同一颗骰子重来是愚蠢的：碰撞本身就是证据，说明抽签的人太多、范围太小。所以失败过的站点会把自己抽签范围的上限（竞争窗口，CW）翻倍，从更宽的范围里抽；再失败就再翻一倍。等待变长，要多花空口时间——但两个人抽到同一个数的概率会迅速下降。下一次成功之后，窗口立刻弹回到最小值。' },
  ],
  numbers: [
    { kind: 'table', heading: '本轮仿真抽出来的数', head: [
      '窗口', '300 ms 内的抽取次数', '平均抽到的时隙数',
    ], rows: [
      ['CW = 15', '770', '7.15'],
      ['CW = 31', '89', '16.07'],
      ['CW = 63', '5', '23.80'],
    ] },
    { heading: '一个时隙值多少', text: '计数器上每一个时隙都是 9 µs 的等待，所以平均等待随窗口大致翻倍：最小窗口下约 64 µs，失败一次之后约 145 µs。' },
    { kind: 'steps', heading: '抽数与倒数，精确版', items: [
      '竞争窗口从下限 15 起步，链路上每一台站点都从这里开始。',
      '当它欠的那段间隙走完、而且这一次确实欠一个抽取时，站点在 0 与 CW 之间均匀地抽一个整数，两端都算在内——CW 为 15 时共十六个可能值，零也是其中之一。这个数就是它的退避计数。',
      '介质每空闲满一个 9 µs 的时隙，这个计数就减一。把它带到零的那个时隙，就是站点发送的那个时隙。',
      '一旦介质变忙——可能是侦听到能量，也可能是它旁听到的某一帧给它装上了预约倒计时——下一次减一就被取消，计数原地冻住。等介质重新安静下来，它先走完该等的间隙，再从同一个数继续；已经等过的部分一点也不作废。',
      '失败的样子就是沉默：期限之前没有回答。站点把这次失败记上一笔，并把窗口放宽到“CW 的两倍再加一”，上限是 1023——于是 15、31、63，一路往上。',
      '连续失败到第七次，这一帧就被放弃：站点不再重传它，窗口也为接下来的事回到下限。这一句话能成立，是因为两个计数器在这里步调一致：一个是这一帧自己的尝试次数，一个是这条队列连续失败的次数；它们分家的那一刻，后面的课会拿一次仿真指给你看。',
      '一次成功之后，失败计数归零，窗口回到下限。另外，只要发送过一次，计数就会清空、并欠下一次新的抽取——所以没有哪台站点能不重新竞争就连发两帧。',
    ] },
    { kind: 'table', heading: '那个期限为什么落在这里', head: [
      '组成', 'µs', '它盖住了什么', '出处',
    ], rows: [
      ['SIFS', '16', '接收端在回答之前理应先停的那一段', '§17.4.4'],
      ['一个时隙', '9', '开口晚了一点的回答', '§17.4.4'],
      ['信号检测时延', '20', '无线电察觉到“有信号开始了”所需的时间', '§17.4.4'],
      ['ACK 超时', '45', '过了这里，这一帧就算丢了', '§10.3.2.9'],
    ] },
    { heading: '第一次碰撞的时刻表', text: '一开始两台站点都发现信道空闲，于是根本没抽签就同时发了出去。重叠在 248 µs 被报出来，期限在 293 µs 到期，两边的窗口都翻倍到 31，新的抽取在 327 µs 完成。' },
    { heading: '出错的频率', text: '在 300 ms 里，这两台站点一共发出 864 帧，碰撞 47 次——大约二十次里错一次。翻倍是管用的：整轮下来，只有五次抽取来自宽达 63 的窗口。' },
  ],
  deeper: [
    { heading: '为什么 AP 什么都没看到', text: 'AP 当时并没有在发送，可它也没有收到一帧乱码。无线电只有在某个前导比空中其余一切都高出一定余量时，才会锁定那一帧。这里两个前导在同一瞬间、以相近的强度开始，互相淹没，于是 AP 根本没有启动任何接收，只记录下信道上有能量。' },
    { heading: '所以这里从来不会有 EIFS', text: '这一点很关键：那段长长的惩罚等待，只有在“接收真的开始了、随后校验失败”时才会启动。压根没锁定过任何前导的站点，没有任何东西可以失败，也就不欠这一段。整轮仿真里一个 EIFS 都没有启动过；重传前的 DIFS 是从 293 µs 超时结束那一刻起算的，而不是从 248 µs 碰撞帧结束那一刻。' },
  ],
  sources: [
    '退避过程见 IEEE Std 802.11-2024 的 §10.3.4.3；aCWmin 15 与 aCWmax 1023 见 §17.4.4，每失败一次窗口翻倍：15 → 31 → 63 → … → 1023。',
    '确认帧的期限见 §10.3.2.9：aSIFSTime + aSlotTime + aRxPHYStartDelay，在这里就是 16 + 9 + 20 µs。',
    '“两个重叠前导里无线电锁住哪一个”所用的捕获余量，是本仿真器的模型取值；随机种子、两台饱和站点与 1528 字节的帧同样如此。',
  ],
  scenario: () => sc(oneRoom(), [
    node('ap', 'AP', 'ap', 5, 4, 'eht', 'idle'),
    node('sta-1', 'STA-1', 'sta', 3.5, 5, 'nonht', 'saturated'),
    node('sta-2', 'STA-2', 'sta', 6.5, 5, 'nonht', 'saturated'),
  ]),
  jumps: [
    J('第一次碰撞', firstCollision),
    J('第一次重传', firstRetry),
    J('第一次退避冻结', firstFreeze),
    J('第一次 CW 翻倍', (r) => r.type === 'CW_CHANGE' && r.cw > 15),
  ],
  observe: [
    '计数器（bo:n）只在信道空闲时递减。对方一发送就冻结，之后从同一个值继续——本轮共有 770 组冻结与恢复，没有一组丢掉过一个时隙。',
    '红色刻度处，AP 泳道上的重叠部分打着斜线并标着“未检测到”。之后检视器里两台站点的 CW 都变成 31，而每一个重传帧都带着 Retry 标志。',
    '跳到大约 8.1 ms 处的第二次碰撞，从那里往回步进：两个计数器在同一个时隙同时归零。碰撞在发生之前的那一刻就已经注定了。',
  ],
  tryThis: [
    '数一数从一个 DIFS 结束到紧随其后那一帧开始之间的空闲时隙。它永远等于那台站点抽到的数。',
    '在编辑器里换一个随机种子再载入。抽到的数不同，碰撞的时刻不同——但出错的比例依旧是大约二十次里一次。',
  ],
  quiz: [
    {
      q: '站点是怎么发现自己那一帧碰撞了的？',
      options: [
        '它在发送时听到了干扰',
        '回答一直没来，等它的那个期限到期了',
        'AP 广播了一条碰撞通知',
      ],
      answer: 1,
      explain: '无线电边说边听是做不到的。正因如此，这套机制是“碰撞避免”，而不是“碰撞检测”。',
    },
    {
      q: '为什么每失败一次就要把窗口翻倍？',
      options: [
        '为了惩罚行为不端的站点',
        '竞争者越多碰撞越多；把抽值摊到更宽的范围上，能把它们重新分开',
        '为了给站点省电',
      ],
      answer: 1,
      explain: '没人知道到底有多少台站点在抢，所以窗口只能用笨办法去学：每失败一次就变宽，每成功一次就弹回去。',
    },
    {
      q: '别的站点在发送时，一个计数器冻结在 7。恢复时它从几开始？',
      options: [
        '重新抽一个数',
        '7——正是它停下的那个数',
        '0，因为等待已经结束了',
      ],
      answer: 1,
      explain: '已经等过的时间从不作废。正是这一点，让等了很久的站点不至于被刚到的站点后来居上。',
    },
  ],
}
