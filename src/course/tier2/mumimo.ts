/**
 * Wi-Fi Tier 2 · M7 · Scheduled Wi-Fi 6/7 · MU-MIMO against OFDMA.
 *
 * Rewritten to the zero-to-hero contract
 * (docs/superpowers/specs/2026-09-21-course-readability-design.md): dividing
 * space instead of frequency, why the access point has to learn where each
 * phone is before it can, why the group here is never three, and when one beats
 * the other. The rule the engine picks by is written out as a procedure in
 * `numbers`, with the turn it picked on run through it row by row; where the
 * trimmed phone turns up next lives in `deeper`, the idealisations in `sources`.
 *
 * The scenario builder and the two variants are unchanged, so the recorded
 * timeline hashes in tests/fixtures/lesson-hashes.json stay byte-identical.
 * Every number quoted below is pinned in tests/course/mumimo.test.ts and in
 * tests/course/quoted-timestamps.test.ts.
 */
import { type Lesson, firstData, firstBa, firstMuDl, J } from '../lessonKit'
import { mumimoScenario } from '../wifiScenes'

export const mumimo: Lesson = {
  id: 'mumimo',
  module: 9,
  title: 'MU-MIMO——按空间而不是按频率划分',
  why: '把信道切成片，一次发送就能照顾到好几部手机，但每一片都只是整条信道的一部分，成员越多，每部手机被服务得越慢。其实还有另一种把大家塞进同一次发送的办法：它不让任何人让出带宽，改用天线（antenna）来付账——这个办法就叫作多用户 MIMO（multi-user MIMO, MU-MIMO）。这一课把两种办法放进同一栋房子里并排比较，看看你会挑哪一种。',
  outcomes: [
    '说出 MU-MIMO 切分的是什么、不切分的又是什么',
    '说出路由器为什么必须先把房间量一遍，才用得了这个办法',
    '在时间轴上读出两个变体，并说出小帧适合哪一种、大帧适合哪一种',
  ],
  needs: ['streams', 'ofdma-dl'],
  terms: [
    { term: 'MU-MIMO', plain: '在一次发送里服务多台设备的另一种办法：给每台瞄准各自的一组空间流，而不是给每台分一片信道' },
    { term: 'beamforming', plain: '用多根天线发同一个信号，彼此错开一点点，使它在某个位置叠加变强、在另一个位置互相抵消' },
    { term: 'sounding', plain: '事先的那次测量：路由器发出一段已知的图案，每台设备把自己听到的样子报回来' },
  ],
  picture: [
    { heading: '切空间，而不是切信道', text: '切片的做法，是把子载波（sub-carrier）分给每个成员一部分，于是每个成员也只拿到速率的一部分。但可以切的不止这一个维度。换成：每个成员都拿到整条信道，只是给每台瞄准不同的一组空间流（spatial stream）——这样谁也不必让出带宽，区分成员靠的是它们在哪儿，而不是它们用哪些音调。这就是 MU-MIMO。' },
    { heading: '但这要求知道每个人在哪儿', text: '同一个信号从多根天线发出去，彼此错开一点点，而这个“一点点”被挑得恰到好处：让它在某部手机处叠加，在另一部手机处互相抵消——这就是波束成形（beamforming）。而要挑出这些延迟，路由器得先把房间量一遍：它发出一段已知的图案，每部手机把听到的样子报回来，这就是探测（channel sounding）。家具会挪，人会走动，量得太旧的结果，瞄准的只是手机从前待过的地方。而本仿真器一次探测也不发，也不为它收空口时间（airtime）：它直接假定瞄得够准。' },
    { kind: 'watch', jump: 0, heading: '去看一眼', text: '载入仿真，跳到第一个同时装着多部手机的发送。在时间轴上方的两个变体之间切换：同一栋房子、同样的流量、同样的手机——只是那个宽块在一边装着三份，在另一边只装两份。' },
    { heading: '这里的分组为什么永远不是三个', text: '瞄准是要拿天线换的。每个成员的空间流都得由路由器自己的天线扛着，所以一个组能有多大，上限就是它的天线数：这台路由器有四根，每部手机协商到两条流，两部手机就把四根占满了。第三部还得再要六条。切片没有这道坎——三部手机各占一片都塞得下——所以空间用尽的地方，频率还走得下去。' },
    { heading: '谁赢', text: '空间是把速率乘上去，只有当数据符号（symbol）本来就够多时，这一乘才划算。频率是把一次发送里那段前导码（preamble）摊薄，而在数据符号本来就没几个时，这一摊最划算。所以许多小帧该去分片，少数几个大帧该去分波束——而那段谁也缩不短的前导码，则在背后默默稀释着两边各自赢来的东西。' },
  ],
  numbers: [
    { kind: 'table', heading: '两种发送各取一次，都来自这栋房子的两个变体——160 MHz，每部手机两条流', head: [
      '变体', '成员数', '每成员负载',
      '数据符号', '整帧长度', '单成员速率',
    ], rows: [
      ['OFDMA', '3', '4,306 B', '3', '92.8 µs', '371.2 Mb/s'],
      ['MU-MIMO', '2', '4,306 B', '1', '65.6 µs', '525.1 Mb/s'],
    ] },
    { kind: 'formula', heading: '一次发送的长度是怎么来的', text: '整帧长度 = 52 µs + 13.6 µs × 数据符号数', note: '这里的 52 µs，是 Wi-Fi 7 帧的 48 µs 前导码加上同样那张 4 µs 的“这一发里有谁”分配表。正交频分多址（orthogonal frequency-division multiple access, OFDMA）的成员只占三分之一子载波，同样的负载要三个数据符号；MU-MIMO 的成员独占整条信道，只要一个——干净的三倍增益，正好等于 MU-MIMO 放弃掉的那个组的大小。可整帧算下来只差 1.41 倍，因为前导码那一段从不缩短。' },
    { heading: '合起来交付的量，方向正相反', text: '两种发送里，每个成员的负载是一样的——都是路由器给那部手机攒下的三个视频帧。三个成员一次交付 12,918 字节，两个成员是 8,612 字节：成员多的那次一趟运得更多，成员少的那次则让每个成员更快拿到自己的东西。你要哪一个，取决于有没有人在等。' },
    { kind: 'steps', heading: '两种办法之间怎么选，一步一步', items: [
      '路由器赢下一轮，列出此刻队列（queue）里有东西、并且和它协商过“切片信道”能力的手机。名单上不到两部，它就照老路只服务一部；否则只留前四部。',
      '只有当每个候选也都协商过 MU-MIMO、而且队首都排着至少 1,000 字节时，它才改成切空间而不是切频率。低于这个量，数据符号本来就没几个，把速率乘上去不值当。',
      '然后它从末尾开始一部一部地裁，直到成员协商的流数之和不超过自己的天线数：两部两流手机要四条，再加第三部就要六条。',
      '裁完后幸存者不足两部，就整组退回切片。否则每个幸存者都拿到整个带宽、各用自己的流数，而被排除在外的手机则等一次属于自己的发送。',
      '无论走哪条路，整次发送的长度都取最长的那个成员所需：48 µs 前导码，再加 4 µs 装“这一发里有谁”的分配表，然后每个符号 13.6 µs。两条路上，每个成员都照旧用自己的两条流：只占三分之一子载波时，4,306 字节要三个符号；独占全部子载波时，只要一个。',
      '每个成员的那一份都在同一瞬间结束，隔 16 µs，一轮确认就把整组了结——切片那个变体的 169 次分组发送里，有 162 次是这样。另外七次，是笔记本压着说了话的那几次。',
    ] },
    { kind: 'table', heading: '三部手机都有排队的那一轮，照着步骤走一遍', head: [
      '步骤', '按频率划分', '按空间划分',
    ], rows: [
      ['队列里有东西的手机', '3', '3'],
      ['都协商过 MU-MIMO，且队首都有 1,000 字节',
        '否', '是'],
      ['三部要的流数，对比四条', '—', '6 > 4'],
      ['裁完后的成员数', '3', '2'],
      ['每个成员拿到的信道', '三分之一', '全部'],
      ['每成员负载', '4,306 B', '4,306 B'],
      ['每成员符号数', '3', '1'],
      ['于是这次发送的长度是', '52 + 13.6 × 3 = 92.8 µs', '52 + 13.6 × 1 = 65.6 µs'],
    ] },
  ],
  deeper: [
    { heading: '被裁掉的那部手机接下来去哪儿了', text: '只要 MU-MIMO 可行，路由器就把组裁到两个候选，第三部单独服务——但“单独服务”究竟是怎么个服务法，值得实测而不是猜。这段仿真里的 196 次两成员发送中，被裁掉的那部手机有 122 次（62%）紧接着就拿到属于自己的单用户发送，有 65 次（33%）出现在下一次组成的配对里，还有 9 次（5%）两样都不是：路由器又先轮到了另外那两部，它只好再等一轮。' },
    { heading: '两个变体真正的差别', text: '只差一个功能开关：MU-MIMO，手机和路由器上都是。“切片信道”那项能力从未被动过，因为把它关掉不会得到“更纯粹的切片”——那会得到两种多用户发送都没有的结果，比较也就无从谈起了。' },
  ],
  sources: [
    '这里比较的两个想法里，MU-MIMO 是更老的那个：下行 MU-MIMO 随 Wi-Fi 5（802.11ac）到来，而 Wi-Fi 6 才加入 OFDMA，并把多用户发送扩展到上行。本仿真器只为 Wi-Fi 6 和 7 模拟多用户发送。',
    '下行 MU-MIMO、它所依赖的探测流程（NDP 通告、NDP、压缩波束成形报告）以及 MU PPDU 的每用户字段，Wi-Fi 6 见 IEEE Std 802.11-2024 第 27 章，Wi-Fi 7 见第 36 章。',
    '本仿真器不真的跑探测流程，也不为它计任何开销：它假设瞄准已经完美，这是模型取值。真实的发射机还要把功率分给组内各成员，对其他成员的置零也不可能精确，所以每个成员的信号质量、连带它的速率，实际上都会有所下降。',
    '48 µs 前导码、多用户信令多出的 4 µs 以及 13.6 µs 符号，是本仿真器为 802.11be PPDU 取的代表值；1,000 字节的门槛与四成员上限则是引擎自己的规定（mac.ts），并非标准正文。',
  ],
  scenario: () => mumimoScenario(false),
  variants: [
    { label: 'OFDMA（按频率划分）', scenario: () => mumimoScenario(false) },
    { label: 'MU-MIMO（按空间划分）', scenario: () => mumimoScenario(true) },
  ],
  jumps: [
    J('第一次多用户发送', firstMuDl),
    J('第一个数据帧', firstData),
    J('第一个 BlockAck', firstBa),
  ],
  observe: [
    '在第一个变体里，把鼠标停在那个宽蓝块上：里面三份，每部手机一份，各占三分之一条信道，速率 371.2 Mb/s。第二个变体里，同样的块只装两份，各自独占整条信道，速率 525.1 Mb/s。',
    '盯住被排除在配对之外的那部手机。多数时候紧接着就有一个属于它自己的发送；有时它出现在下一次配对里；偶尔两样都没有，它得再等一轮。被排除的是哪一部并不固定。',
    '两个变体里，每个成员的那一份都在同一瞬间结束，隔一小段，一轮确认就把整组了结——除非笔记本压着这次发送说了话，这样的情况只有几次。',
  ],
  tryThis: [
    '在编辑器里加上第四部手机。切片那个变体会把它吸收进来，分组变成四个成员、每人四分之一片；再加第五部，分组就不再长大了，因为本引擎把一组封顶在四个。而无论你加多少部，MU-MIMO 那个变体都长不过两个。',
    '关掉笔记本的备份流量再重新载入。路由器现在能在下一个视频包到达之前就把当前这部手机的包送完，于是两种多用户（multi-user, MU）发送都变得罕见——已经没有什么可以凑成一组了。',
  ],
  quiz: [
    {
      q: '三部手机，一次发送。在切片的办法下，再加入第四部之后，每部手机的速率会怎样？',
      options: [
        '下降——信道又被多分了一份',
        '不变——每个成员都拿到整条信道',
        '上升——成员越多，每人分摊的开销越少',
      ],
      answer: 0,
      explain: '切片的成员分享的是子载波，而不是时间：多一个成员，每个人的那一片都更薄。这正是 MU-MIMO 不肯付的代价，它改用天线来付。',
    },
    {
      q: '为什么一台四天线的路由器不能用 MU-MIMO 同时服务三部两流手机？',
      options: [
        '三路瞄准的信号会在空口里互相碰撞',
        '它们的流数加起来是六条，比路由器拥有的多出一半',
        'MU-MIMO 分组永远只能有两个成员',
      ],
      answer: 1,
      explain: '每部手机协商到两条流，两部手机就用满了全部四条。分组会一直被裁到塞得下为止，这里正好落在两个——第三部手机则被单独服务。',
    },
  ],
}
