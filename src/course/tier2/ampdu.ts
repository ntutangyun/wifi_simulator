/**
 * Wi-Fi Tier 2 · M3 · QoS and efficiency · A-MPDU, paying the ceremony once.
 *
 * Rewritten to the zero-to-hero contract
 * (docs/superpowers/specs/2026-09-21-course-readability-design.md) and grown
 * from the 240-word original: what a batch under one preamble looks like, the
 * one answer that covers all of it, and what happens to a bad member of the
 * batch. The ceiling, the delimiters and the simulator's own bluntness live in
 * `deeper`; the clause numbers in `sources`.
 *
 * The scenario builder and the variant are unchanged, so the recorded timeline
 * hashes stay identical. Every number quoted below is pinned in
 * tests/course/ampdu.test.ts.
 */
import { type Lesson, oneRoom, node, sc, firstBa, firstAmpdu, J } from '../lessonKit'

export const ampdu: Lesson = {
  id: 'ampdu',
  module: 2,
  title: 'A-MPDU——竞争一次，发一批',
  why: '赢下信道是件贵事，而这笔钱和你随后发多少毫无关系。等待、倒数、帧头那段图案、末尾那个回答，无论这一帧几乎是空的还是塞得满满当当，价钱都一样。随着电台越来越快，数据在这笔固定开销面前越缩越小，最后一轮里大半时间都花在了排场上。这一课我们看一台站点（STA）如何买下一轮，并且把它用好。',
  outcomes: [
    '解释为什么电台越快，一轮的固定开销反而越难受',
    '说清一个前导下面那一批里装着什么，以及它是怎么被回答的',
    '读出一帧成功送达要花多少空口时间——打批和不打批各是多少',
  ],
  needs: ['airtime', 'frame-anatomy', 'retries-queues'],
  terms: [
    { term: 'A-MPDU', plain: '一批帧首尾相接打成一包，在同一个前导下面作为一次传输发出去' },
    { term: 'subframe', plain: '这一批里的其中一帧：它保留自己的帧头和自己的校验，也单独接受判决' },
    { term: 'BlockAck', plain: '块确认：一个简短的回答就覆盖整批，里面为这一批的每一个成员留了一位' },
  ],
  picture: [
    { heading: '排场的价钱两边一样', text: '把空口上的一轮想成“固定价 + 浮动价”。静默、倒数、让接收端锁住的那段前导、末尾那个回答，都是固定的那部分；只有净荷随你发的东西变大。换一档更快的编码，净荷会缩——可固定的那部分纹丝不动，因为前导是用人人都听得见的慢速率发的，而那段等待不管你随后说得多快，价钱都一样。' },
    { heading: '好几帧，一个前导', text: '所以，与其把一整轮花在一帧上，发送方干脆把已经排在队列里、发往同一个接收端的帧全拿过来，首尾相接排好，作为一次传输、跟在同一个前导后面发出去。这一批就是 A-MPDU。里面的东西并没有被合并：每一帧都保留自己的帧头和自己的校验；这一批里的一个成员，就是一个子帧，而每个子帧身前还有一小段标记报出它有多长。' },
    { kind: 'watch', jump: 0, heading: '打开一批看看', text: '载入仿真，跳到第一批。它带着一个角标，上面是这一批里帧的数量；把鼠标悬上去，看看这个数量和总字节数，再看看这一轮里花在其他事情上的时间有多少。' },
    { heading: '一个回答管住全部', text: '如果一个个单独回答，刚省下来的开销就又长回来了：每个成员都要一段停顿加一个小帧。于是接收端改成整批只回一个回答，而这个回答就是 BlockAck。它比一个普通确认帧长不了多少，却带着一张位图，每个成员占一位：这个到了，那个没到。一次停顿，一个回答，整批就结清了。' },
    { heading: '当其中一个坏了', text: '正因为每个成员都是单独校验的，坏掉一个并不连累其余。它在位图里的那一位保持为空，只有这一帧重新排队——它甚至可以搭上下一批，和新来的帧一起走。这一点恰恰是本仿真器没有建模的：在这里，一批里任何位置发生碰撞，整批都会丢，所以繁忙房间看起来会比真实情况更难堪一些。' },
    { heading: '它没有改变的东西', text: '这里没有任何东西被发得更快。编码没变，距离没变，字节还是那些字节。变的只有一个比例：房间为一轮付出的代价，和这一轮真正送到的东西之间的比例——这也是为什么聚合是现代 Wi-Fi 里最便宜的一笔大收益，为什么每台设备都不用谁吩咐就这么做。' },
  ],
  numbers: [
    { kind: 'table', heading: '同一轮，两种打法', head: [
      '一轮之内', '打批', '一帧一发',
    ], rows: [
      ['发出的帧数', '14', '1'],
      ['这次传输', '21 502 B · 2 248 µs', '1 530 B · 200 µs'],
      ['回答', 'BlockAck · 32 B · 32 µs', 'ACK · 14 B · 28 µs'],
      ['开场交互', 'RTS + CTS · 28 µs each', '—'],
      ['每成功一帧的发送时间', '166.9 µs', '228.0 µs'],
    ] },
    { kind: 'formula', heading: '最后一行是怎么算的', text: '打批：   (28 + 28 + 2 248 + 32) µs ÷ 14 帧 = 166.9 µs\n一帧一发：(200 + 28) µs ÷ 1 帧      = 228.0 µs', note: '同样的编码，同样的字节，同一个房间。省下来的，全都在“不用再发的那些东西”上。' },
    { kind: 'table', heading: '整轮仿真，两种打法', head: [
      '200 ms 之内', '打批', '一帧一发',
    ], rows: [
      ['赢下的轮数', '81', '83'],
      ['成功送达的帧数', '1 120', '738'],
      ['每成功一帧的发送时间', '168.9 µs', '228.3 µs'],
    ] },
    { heading: '为什么实测和算式不完全一致', text: '两种打法赢下的轮数几乎一样多——差别在于每一轮装了多少。实测值比算式稍高一点，是因为有少数几轮丢了要重来；两边的编码完全相同，所以这份收益里没有一丁点藏在速率上。' },
    { kind: 'steps', heading: '一批是怎么攒起来、又怎么被回答的', items: [
      '电台先拿队列最前面那一帧，看它是发给谁的。能跟着一起走的，必须是发往同一个接收端的帧，而且按队列里的先后顺序取。',
      '它一帧一帧往里加，最多 64 帧；一旦下一帧会把这次传输拉得比 5.484 ms 还长，或者把整次交互——这一批、那段停顿、那个回答——顶到本轮末尾之外，就停在这一帧之前。队头那一帧总是能走，实在装不下别的就它一个走。',
      '然后把选中的帧首尾相接排好。每一帧都成为一个子帧：先是 4 字节的定界符，里面写着长度，接着是这一帧自己的 QoS 帧头、它的净荷和它的校验，最后补齐到四字节边界。最后一个子帧不补齐。',
      '整串子帧作为一次传输发出去：一个前导，一种编码。',
      '一个 SIFS 之后，接收端只回一个 32 字节的 BlockAck；按标准，它为每一个子帧带一位——到了的置位，没到的置空。',
      '本仿真器要粗糙些：它把这一批当成一个整体判定。若到确认超时为止都没等来回答，这一批里的每一帧都记一次重传，回到队列最前面等着搭下一批，窗口也像碰了一次那样翻倍。',
    ] },
    { kind: 'table', heading: '第一批，逐项加起来', head: [
      '步骤', '数值',
    ], rows: [
      ['取走的帧数，允许的是 64', '14'],
      ['前十三个每个：4 + 26 + 1 500 + 4，再补齐', '1 536 B'],
      ['最后一个，不补齐', '1 534 B'],
      ['13 × 1 536 + 1 534', '21 502 B'],
      ['在空中的时长', '2 248 µs'],
      ['整次交互：提问、回答、三段停顿、这一批、BlockAck', '2 384 µs'],
      ['本轮的上限', '2 528 µs'],
      ['剩下的，而第十五个子帧要花约 160 µs', '144 µs'],
    ] },
  ],
  deeper: [
    { heading: '一批的大小由什么决定', text: '两个上限碰在一起，低的那个说了算。一个是结构上的：这里一个聚合最多容纳 64 个成员，总长度还受接收端答应缓存多少的约束。另一个是时钟：这一批、它的开场交互和它的回答，必须全部装进获胜者被允许占用信道的那段时间里。本场景中先顶到的是时钟，所以这一批是 14 帧，而不是 64 帧。' },
    { heading: '成员之间的那段标记', text: '每个成员前面都有一小段定界符，里面写着它的长度以及对这个长度的校验，后面还要补齐，让下一个成员从四字节边界开始。正是这一点，让漏掉某个成员开头的接收端可以直接跳到下一个，而不是把这一批剩下的全都丢掉；也正因为如此，这里的一批是 21 502 字节，而不是 14 × 1 530。' },
    { heading: '必须先存在的那份约定', text: '接收端不可能为一批它从未答应缓存的帧回一张位图。在这一切之前，两端要先交换一次简短的请求与响应，把这份约定谈好：覆盖哪一类流量、最多允许多少帧在途、窗口从哪里开始。本仿真器假定这份约定早已谈妥，所以你在时间轴上看不到它。' },
  ],
  sources: [
    'A-MPDU 聚合、定界符、补齐到四字节边界，以及这里用的 64 个子帧上限，见 IEEE Std 802.11-2024 的 §10.12 与 §9.7；BlockAck 帧及其位图见 §9.3.1.9，先于它的那份约定见 §10.25。',
    '“聚合中任意一处发生碰撞就整批丢失”是本仿真器的模型取值：标准是按每个子帧各自的比特来确认的。随机种子、这台唯一的上传站点、1500 字节的帧，以及本轮最终采用的编码，同样都是模型取值。',
  ],
  scenario: () => sc(oneRoom(), [
    node('ap', 'AP', 'ap', 5, 4, 'eht', 'idle'),
    node('sta-1', 'Uploader', 'sta', 6.5, 5, 'vht', 'saturated'),
  ]),
  variants: [
    {
      label: '关闭聚合（同一设备）',
      scenario: () => sc(oneRoom(), [
        node('ap', 'AP', 'ap', 5, 4, 'eht', 'idle'),
        node('sta-1', 'Uploader', 'sta', 6.5, 5, 'vht', 'saturated', { edca: true, txop: true }),
      ]),
    },
  ],
  jumps: [
    J('第一个 A-MPDU', firstAmpdu),
    J('第一个 BlockAck', firstBa),
  ],
  observe: [
    '这里的每一批都带着 ×14 的角标。悬停其中一个：十四帧、21 502 字节在同一个前导下面发出去，是一次时长 2 248 µs 的传输。',
    '一个淡紫色的 BlockAck，32 字节、32 µs，就把这一轮收尾了——它顶替的是十四个独立回答，以及每个回答前面的那段停顿。',
    '载入“关闭聚合”的变体：同一台设备、同样的编码，现在改成一帧一发的 1 530 字节帧，每一帧都要自己那个 28 µs 的回答。',
  ],
  tryThis: [
    '在检视器里看队列每赢一次信道就清掉 14 帧，而不是 1 帧。14 是“一次获胜允许持续的时间”装得下的数量，并不是聚合的上限——上限是 64 帧。',
    '比较两个变体的吞吐量。同样的编码、同样的距离，这里成功送达的帧数约为 1.5 倍；这个差距只反映聚合本身的贡献，因为两个变体赢下一轮的频率差不多。',
  ],
  quiz: [
    {
      q: '这份收益主要来自哪里？',
      options: [
        '打批之后用了更高的编码速率',
        '把一轮的固定开销摊到许多帧上，而不是只摊在一帧上',
        '批里的帧头更短了',
      ],
      answer: 1,
      explain: '对比的两边编码完全相同。变的只是“排场与数据”的比例。',
    },
    {
      q: '一批里有一帧校验没过。标准会怎么做？',
      options: [
        '整批重发',
        '它在回答里的那一位保持为空，只有这一帧重新排队',
        '接收端另发一条请求专门要它',
      ],
      answer: 1,
      explain: '每个成员都是单独校验的，所以回答可以很精确。本仿真器要粗糙一些，会把整批丢掉。',
    },
  ],
}
