/**
 * Wi-Fi Tier 2 · M4 · Capacity knobs · Spatial streams.
 *
 * Rewritten to the zero-to-hero contract
 * (docs/superpowers/specs/2026-09-21-course-readability-design.md): several
 * antennas sending different words at once, why both ends must have them, and
 * why the widest channel with the most streams is no shorter than the widest
 * channel alone. It loads `width`'s own builder at its narrowest setting, and
 * its last variant is `width`'s widest, so the two lessons are one experiment.
 *
 * The scenario builder and the five variants are unchanged, so the recorded
 * timeline hashes in tests/fixtures/lesson-hashes.json stay byte-identical.
 * Every number quoted below is pinned in tests/course/streams.test.ts.
 */
import { type Lesson, firstData, firstAck, J } from '../lessonKit'
import { widthScenario } from '../wifiScenes'

export const streams: Lesson = {
  id: 'streams',
  module: 3,
  title: '空间流——同一瞬间说出好几句话',
  why: '上一课是用“地方”换速度：给一块更宽的频段，同样一车比特就多了几条车道。还有第二种买法，而且一点额外的频段都不用花。给链路的两端各装上更多天线，它们就能在同一瞬间、频段上的同一个位置说出不同的话，接收端照样分得开。这一课讲的是：这样买到了什么，它对两端各有什么要求，以及它到什么地方就买不到东西了。',
  outcomes: [
    '说出多根天线怎样在同一批子载波上同时发出不同的信号',
    '根据两端各有几根天线，算出这条链路实际跑几条流',
    '说清带宽与空间流这两个倍数里，哪个要拿信号去换，哪个是白送的',
  ],
  needs: ['width'],
  terms: [
    { term: 'spatial stream', plain: '在同一瞬间、同一批子载波上发出的好几路信号中的一路，靠各自走过的路径区分开来' },
    { term: 'antenna', plain: '一根辐射单元；电台想发或想收几条流，就得有几根' },
  ],
  picture: [
    { heading: '同一瞬间的好几句话', text: '信道变宽，是给电台在频段上更大的地方；而天线，是把同一块地方再用一遍。两端各装两根天线时，发送端就在同一瞬间、同一批子载波上放出两路不同的信号，接收端照样能把它们分开——因为两路信号是沿着屋里不同的路径过来的。这样并行的每一路就是一条空间流，它让一块信号里装的比特数翻倍，和子载波变多的效果一模一样。' },
    { heading: '两端都有发言权', text: '这事两端都得有辐射单元，也就是天线：想发几条流就得有几根，想把几条流分开也得有几根。所以一条链路按两端里较小的那个流数运行。四根天线的路由器对上两根天线的手机，就是一条两流链路；而路由器多出来的那一对并没有浪费——它只是得另外找个人说话。' },
    { kind: 'watch', jump: 0, heading: '去看一眼', text: '载入仿真，跳到第一个数据帧。这一课打开的是单流；上面的按钮把它切到两条流、四条流，再切到两个混合的情形。变的只有那个蓝色数据块。' },
    { heading: '白送的那个倍数', text: '和带宽的区别在于价钱。信道变宽，接收端就要多收进一份噪声；而多加一条流不会——信道还是原来那么大，噪声地板不动，速率阶梯上每一级所要的信号也不动。在本仿真器里——在一间满是反射的屋子里也差不多——空间流是没人跟你收钱的那个倍数；带宽那个倍数，每翻一倍都要付 3 dB。' },
    { heading: '倍数到头的地方', text: '倍数只在还有东西可分的时候才有用。把最宽的信道和最多的流一起用上，帧并不会比最宽信道单独上阵时更短：它那时就已经只剩一个符号，而一个符号就是地板。在那里空间流什么也没买到——而宽信道多收进来的噪声一分不少，只是这笔钱彻底白花了。' },
  ],
  numbers: [
    { kind: 'table', heading: '同一个 1500 字节的帧（空口上 1530 字节），20 MHz，MCS 13', head: [
      '空间流', '每符号比特数',
      '符号数', '空口时间',
    ], rows: [
      ['1', '2340', '6', '129.6 µs'],
      ['2', '4680', '3', '88.8 µs'],
      ['4', '9360', '2', '75.2 µs'],
    ] },
    { kind: 'table', heading: '两个混合变体', head: [
      '变体', '链路实际跑的流数',
      '空口时间', '等同于',
    ], rows: [
      ['路由器 4 · 手机 2', '2', '88.8 µs', '上表里 2 条流那一行'],
      ['160 MHz · 4 条流', '4', '61.6 µs', '上一课里单靠 160 MHz 的结果'],
    ] },
    { heading: '弱的一端说了算', text: '所有能力都要按较弱的那一端协商，而这个混合变体正是这种搭配：它落在两流的 88.8 µs 上，而不是四流的 75.2 µs。逐帧去比，它和纯粹的两流那一次跑完全看不出区别。' },
    { heading: '以及它到头的地方', text: '在四条流之上再加八倍的子载波，只换来一个符号：61.6 µs 对四流的 75.2 µs。那一帧本来就只剩两个符号，而第二个符号，是最后还能拿走的东西。' },
    { kind: 'steps', heading: '多一条流到底改变了什么，一步一步', items: [
      '两端各自报出自己能跑几条流——有几根天线就是几条——链路取两者中较小的那个数。',
      '照上一课的办法选级。信道并没有变宽，噪声地板没动，每一级的要求也没动：流数根本就不是这一步的输入。',
      '每符号比特数 = 该级在 20 MHz 下的每符号比特数，乘以带宽的子载波倍数，再乘以流数。多一条流，效果和多一份子载波完全一样。',
      '符号数 = 这一帧的比特数除以它，再向上取整成整数个符号。如果多出来的那条流只是削掉最后那个没装满的符号，它就什么也没买到。',
      '空口时间 = 48 µs 的前导，加上每个符号 13.6 µs。无论跑几条流，前导都按同样的方式发出去，所以它从不变短。',
    ] },
    { kind: 'table', heading: '四条流，以及那个混合搭配，照着步骤走一遍', head: [
      '步骤', '4 条流', '路由器 4 · 手机 2',
    ], rows: [
      ['两端各能跑几条', '4 和 4', '4 和 2'],
      ['于是链路跑', '4', '2'],
      ['级别，不受流数影响', 'MCS 13', 'MCS 13'],
      ['每符号比特数：20 MHz 的值 × 流数', '2340 × 4 = 9360', '2340 × 2 = 4680'],
      ['符号数：⌈12262 ÷ 它⌉', '2', '3'],
      ['空口时间：48 + 13.6 × 符号数', '75.2 µs', '88.8 µs'],
    ] },
  ],
  deeper: [
    { heading: '多出来的那一对流做什么用', text: '四根天线的路由器对上两流的手机，并不意味着一半硬件只能闲着。它可以在同一瞬间把另外两条流指向第二部手机，给两边各发各的数据——这就是 MU-MIMO（多用户 MIMO），本模块后面有专门的一课。它需要的是“还有第二台站点正等着发东西”，所以这份好处只在热闹的房子里看得见，在这张桌子上看不见。' },
    { heading: '为什么路径必须不一样', text: '把几条流分开，本质是解线性方程：接收端解一个小方程组，每根接收天线给出一行。只有当这些行真的互不相同——也就是几路信号真的沿着明显不同的路径到达——方程才解得开。反射很多的房间（住宅、办公室）对空间流很友好；空旷野地上干干净净的直视路径则是最差情形，那里第二条流几乎一文不值。' },
  ],
  sources: [
    '每符号比特数来自 802.11be 的参数集：最高调制下 20 MHz 的一个符号装 2340 个数据比特，再乘以流数（IEEE Std 802.11-2024 第 36 章及其修正案）。',
    '“链路按两端较小的流数运行”出自 §9.4.2.x 与 §11.3 的能力协商；本仿真器把它建模为两者取最小，且不做逐流回退。',
    '仿真器让每条流的质量都一样：天线之间不相关，直视路径下也不掉秩。现实里小房间中的四流链路，很少真能拿到完整的倍数。',
  ],
  scenario: () => widthScenario(20, 1),
  variants: [
    { label: '1 条流', scenario: () => widthScenario(20, 1) },
    { label: '2 条流', scenario: () => widthScenario(20, 2) },
    { label: '4 条流', scenario: () => widthScenario(20, 4) },
    { label: '路由器 4 · 手机 2', scenario: () => widthScenario(20, 2, 4) },
    { label: '160 MHz · 4 条流', scenario: () => widthScenario(160, 4) },
  ],
  jumps: [
    J('第一个数据帧', firstData),
    J('第一个 ACK', firstAck),
  ],
  observe: [
    '从一条流到两条流：数据部分正好减半，81.6 µs 变 40.8 µs。从两条到四条本该再减半到 20.4 µs，但帧只能按整数个符号发出去，所以停在 27.2 µs。',
    '每个变体的“速率”一行都是 MCS 13：一条流也好四条流也好，20 MHz 也好 160 MHz 也好。空间流复用的是同一批子载波，所以它永远拿不走一档调制；而在这张桌子上，连最宽信道多收的那份噪声也没拿走。',
    '“路由器 4 · 手机 2”和“2 条流”看不出区别：同样是 88.8 µs，同样的块出现在同样的位置。',
  ],
  tryThis: [
    '把“2 条流”和“路由器 4 · 手机 2”并排放在一起，在时间轴上找不同。找不到——路由器多出来的那一对流没有对象可说话。',
    '再把这里的“2 条流”和上一课的 40 MHz 变体比一比：同一个帧，同样的 88.8 µs。但两者之中，只有一个向笔记本多要了 3 dB 的信号。',
  ],
  quiz: [
    {
      q: '为什么加一条空间流不必向发送端多要信号，而带宽翻倍就要？',
      options: [
        '空间流的发射功率更高',
        '信道宽度没变，接收端收进的噪声也没变——两条流靠空间区分，不靠频率',
        '多出来的那些流是在另一条信道上发的',
      ],
      answer: 1,
      explain: '噪声功率跟着信道宽度走。带宽翻倍，噪声也翻倍，这就是那 3 dB；而第二条流复用的是同一批子载波，噪声地板不动，速率阶梯上每一级所要的信号也纹丝不动。',
    },
    {
      q: '一台四根天线的路由器服务一部两根天线的手机。这条链路用几条流？',
      options: [
        '四条——路由器说了算',
        '两条——链路按两端里较小的流数运行',
        '三条——取两端的平均',
      ],
      answer: 1,
      explain: '所有能力都要按较弱的一端协商。链路用两条流，帧要 88.8 µs——两流的时间，而不是四流的 75.2 µs。',
    },
  ],
}
