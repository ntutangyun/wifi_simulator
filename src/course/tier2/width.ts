/**
 * Wi-Fi Tier 2 · M4 · Capacity knobs · Channel width.
 *
 * Rewritten to the zero-to-hero contract
 * (docs/superpowers/specs/2026-09-21-course-readability-design.md): a wider
 * channel is more lanes, not a faster car; what the extra lanes cost in noise
 * and in neighbours; why the preamble of a frame never shrinks. The one-decibel
 * inversion window and the legacy fallback are in `deeper`; the numerology and
 * the clause numbers are in `sources`.
 *
 * The scenario builder and the four variants are unchanged, so the recorded
 * timeline hashes in tests/fixtures/lesson-hashes.json stay byte-identical.
 * Every number quoted below is pinned in tests/course/width.test.ts.
 */
import { type Lesson, firstData, firstAck, J } from '../lessonKit'
import { widthScenario } from '../wifiScenes'

export const width: Lesson = {
  id: 'width',
  module: 3,
  title: '信道带宽——多开几条车道，而不是换一辆快车',
  why: '到目前为止的每一课讲的都是怎么分享空口。这一课讲的是：轮到自己发的时候，一帧能从空口里拿到多少。可以给一条链路划一块更宽的频段，同一帧发完所需的时间就更短。这是最简单的一个提速旋钮，但它带着一张账单：频段越宽，混进来的噪声也越多。同一个旋钮，在书桌旁能把帧变短，到了远端墙边却可能把链路整个弄断。',
  outcomes: [
    '说出带宽翻倍为什么从来不会让一帧的时间减半',
    '在时间轴上读出同一帧在四种带宽下各自的空口时间',
    '为一台处在覆盖边缘的站点（STA），在“宽”和“稳”之间做出选择',
  ],
  needs: ['decode-thresholds', 'airtime'],
  terms: [
    { term: 'channel width', plain: '一条链路占用的频段有多宽：20、40、80 或 160 MHz' },
    { term: 'sub-carrier', plain: '信道被切成的一根根窄音调中的一根，每根每次驮几个比特' },
    { term: 'symbol', plain: '一段等长的信号块；一帧的数据永远是整数个这样的块发出去的' },
    { term: 'noise floor', plain: '没人说话时，接收端听到的那份背景功率' },
  ],
  picture: [
    { heading: '多开车道，不是换快车', text: '信道变宽，并不是让电台说得更快，而是让它一次有更大的地方可说。一条链路占用的频段有多宽，就是信道带宽。Wi-Fi 把手里那块频段切成一根根很窄的音调，每一根就是一个子载波，每根装的比特数都一样；所以带宽翻倍，子载波数就翻倍，每一块信号里装的比特数也翻倍。车并没有变快，是路上多了几条车道，同样一车比特用更少的块就跑完了。' },
    { heading: '从来不会变短的那一段', text: '一帧里只有数据那一段是骑在子载波上的。它前面还有前导——接收端用来锁住这一帧的那段固定图案——在任何带宽下都一样长。而数据是按等长的信号块发出去的，一块就是一个符号；装不满一块的零头，也要向上凑成一整个符号。所以信道变宽，缩短的只是能缩的那一段，其余原地不动。' },
    { kind: 'watch', jump: 0, heading: '去看一眼', text: '载入仿真，跳到笔记本发给路由器的第一个数据帧——屏幕上那台路由器，就是这套房子里大家都跟它说话的那一台。这一课打开的是最宽的一档；用上面的按钮逐档调窄再调回来，盯住那个蓝色数据块——只有它在变长变短，周围一切纹丝不动。' },
    { heading: '账单付的是噪声', text: '门开得越大，进来的东西就越多，屋里的背景嘶声也一样。没人说话时接收端听见的那份背景功率，就是噪声地板；带宽翻倍，它收进的噪声功率也翻倍，于是速率阶梯上的每一级，现在都要更强的信号才撑得住。它买到了什么、买不到什么要分清：对“另一台站点压着你说话”毫无帮助——对方的信号会随带宽一起变大，和你自己的一模一样。宽信道花掉的，是覆盖距离。' },
    { heading: '更宽反而更慢的时候', text: '这张账单有时会大过货品本身。往屋子边缘走，宽信道要的信号比这个位置拿得出来的更多，于是链路掉到更慢的一级，同一帧发出来反而比在窄信道上更长。子载波翻一倍，补不回速率掉了一半还多的窟窿。' },
    { heading: '还有邻居', text: '信道宽，在所有邻居耳朵里也一样宽：它盖住的频段更多，于是楼里更多的其它网络得等你，你也得等它们。空口很空的时候，带宽是白捡的便宜；可在一栋住宅楼里，纸面上最快的那一档，到了真实的房间里反倒最慢。' },
  ],
  numbers: [
    { kind: 'table', heading: '同一个 1500 字节的帧（空口上 1530 字节），Wi-Fi 7，单流，就在书桌上', head: [
      '带宽', '数据子载波',
      'MCS', '速率一行',
      '符号数', '空口时间',
    ], rows: [
      ['20 MHz', '234', '13', '172.1 Mb/s', '6', '129.6 µs'],
      ['40 MHz', '468', '13', '172.1 Mb/s', '3', '88.8 µs'],
      ['80 MHz', '980', '13', '172.1 Mb/s', '2', '75.2 µs'],
      ['160 MHz', '1960', '13', '172.1 Mb/s', '1', '61.6 µs'],
    ] },
    { kind: 'formula', heading: '空口时间花在哪儿', text: '空口时间 = 48 µs + 13.6 µs × ⌈(16 + 8·字节数 + 6) ÷ (20 MHz 下每符号比特数 × 子载波倍数)⌉', note: '前导那一段永远不动；式子里的 16 和 6，是每一帧都要在负载之外添上的服务位与尾位；向上取整的括号把零头凑成一个完整符号。80 MHz 给的子载波比 20 MHz 的四倍还多一点点，因为信道两端那截安静的边沿是整块频段只付一次。' },
    { heading: '带宽在噪声上的代价', text: '带宽每翻一倍，收进来的噪声功率也翻一倍——3 dB——所以这里最宽的一档要比最窄的一档多约 9 dB 信号。在这张桌子上这笔钱还付得起：链路稳稳停在最高一级，信噪比 48.7 dB，而那一级要的是 48.0 dB。' },
    { kind: 'table', heading: '同一台笔记本，挪到客厅中间', head: [
      '带宽', 'MCS',
      '空口时间', '是否送达',
    ], rows: [
      ['20 MHz', '3', '415.2 µs', '帧帧送达'],
      ['40 MHz', '3', '238.4 µs', '帧帧送达'],
      ['80 MHz', '2', '170.4 µs', '帧帧送达'],
      ['160 MHz', '0', '224.8 µs', '帧帧送达——却比 80 MHz 还慢'],
    ] },
    { kind: 'steps', heading: '带宽到底改变了什么，一步一步', items: [
      '先取两端都跑得动的带宽：链路用的是两者中较窄的那个，后面每一步都在这个带宽上算。',
      '算出这个带宽下的噪声地板：这段带宽上的热噪声，加上接收机的噪声系数。20 MHz 是 −93.99 dBm，每翻一倍再加 3.01 dB。',
      'SNR = 这条链路的 RSSI 减去那个噪声地板：RSSI 没变，所以信道越宽，剩给你的越少。',
      '沿着速率阶梯往上走，留住最后一个满足“所需 SINR + 3 dB 速率余量 ≤ SNR”的级。每一级的要求与带宽无关——变的只有噪声地板。',
      '每符号比特数 = 该级在 20 MHz 下的每符号比特数，乘以第一张表里那个带宽的子载波倍数。',
      '符号数 = 这一帧的比特数除以它，再向上取整成整数个符号——就是上面那条式子。空口时间就是 48 µs 的前导，加上每个符号 13.6 µs。',
    ] },
    { kind: 'table', heading: '客厅那台笔记本，在最宽的两档上照着步骤走一遍', head: [
      '步骤', '80 MHz', '160 MHz',
    ], rows: [
      ['这条链路的 RSSI', '−70.51 dBm', '−70.51 dBm'],
      ['减去这个带宽下的噪声地板', '−87.97 dBm', '−84.96 dBm'],
      ['= SNR', '17.46 dB', '14.45 dB'],
      ['含余量后放得下的最高一级', 'MCS 2', 'MCS 0'],
      ['它要求', '13.99 + 3 = 16.99 ✓', '8.99 + 3 = 11.99 ✓'],
      ['再上一级要求', '16.99 + 3 = 19.99 ✗', '11.99 + 3 = 14.99 ✗'],
      ['每符号比特数：20 MHz 的值 × 倍数', '351 × 4.19 = 1470', '117 × 8.38 = 980'],
      ['符号数：⌈12262 ÷ 它⌉', '9', '13'],
      ['空口时间：48 + 13.6 × 符号数', '170.4 µs', '224.8 µs'],
    ] },
    { heading: '倒挂，一句话说清', text: '最后三行就是倒挂。最宽的那一档差半个分贝够不着上一级，落点比 80 MHz 撑住的那一级低了两级：阶梯上有一级只比下一级高 2 dB，而噪声地板抬高 3 dB 的这一步，正好把它整个跨了过去。子载波恰好翻倍，补不回每符号比特数只剩三分之一的亏空。' },
  ],
  deeper: [
    { heading: '只有一分贝宽的窗口', text: '上面那个倒挂，两边都十分惊险。“160 MHz 确实比 80 MHz 慢，而两者又都还能把每一帧解出来”的接收功率区间，只有约一分贝宽。客厅那个位置落在 −70.51 dBm，靠近这个区间的正中，离两端各约半分贝。把笔记本再挪近一点，160 MHz 又赢回来；再挪远一点，它就彻底送不到了。' },
    { heading: '根本没有宽信道模式的电台', text: '在最远的角落里，到编辑器把笔记本改成 802.11a。传统电台根本没有宽信道模式，链路只能退回 20 MHz——于是确认帧回来了，每帧 704 µs、18 Mb/s：是同一帧在书桌上所花空口时间的五倍半，但它送到了。又窄又老，也强过又宽又没声。' },
  ],
  sources: [
    '234、468、980、1960 这几个数据子载波数，来自 802.11be PPDU 的 OFDM 参数集（IEEE Std 802.11-2024 第 36 章及其修正案）；空口时间跟着这些子载波数走，而不是跟着兆赫数走。',
    '48 µs 的前导与 13.6 µs 的符号，是本仿真器为这类 PPDU 取的单一代表值，并非逐字段相加的结果；它们代入的 TXTIME 公式见 §17.4.3。',
    '“每翻一倍 3 dB”出自热噪声公式 kTB，噪声系数用的是本仿真器自己的取值；每一级的灵敏度——包括让倒挂得以发生的那一档 2 dB 窄阶——出自第 36 章的最小输入灵敏度表。',
  ],
  scenario: () => widthScenario(160, 1),
  variants: [
    { label: '20 MHz', scenario: () => widthScenario(20, 1) },
    { label: '40 MHz', scenario: () => widthScenario(40, 1) },
    { label: '80 MHz', scenario: () => widthScenario(80, 1) },
    { label: '160 MHz', scenario: () => widthScenario(160, 1) },
  ],
  jumps: [
    J('第一个数据帧', firstData),
    J('第一个 ACK', firstAck),
  ],
  observe: [
    '把四种带宽逐档切过去，盯住同一个蓝色数据块：129.6 µs、88.8、75.2、61.6。带宽翻倍从来不会让一帧减半——每一帧里那段前导，不随任何东西缩短。',
    '把这四个数字各减掉固定的 48 µs 前导，剩下的数据分别是 81.6、40.8、27.2 和 13.6 µs。从头到尾只有这一段在动，而到了最宽的一档，它只剩一个符号。',
    '四档里白色的 ACK 完全一样，“速率”那一行也从不动：四种带宽都是 172.1 Mb/s，因为它报的是一组 20 MHz 子载波能装多少。带宽体现在空口时间里，从不体现在速率那一行。',
  ],
  tryThis: [
    '点“在编辑器中打开”，把笔记本拖到路由器右边七格半、下面两格，大约客厅正中。四种带宽在那里仍然帧帧送达，但最宽的一档已经不是最快的了：上面第二张表说的就是这个位置。',
    '继续挪到最远的角落。在最宽的一档，一个 ACK 也回不来。把带宽逐档调窄，链路就回来了——80 MHz 每帧 401.6 µs，20 MHz 524.0 µs，而 40 MHz 卡在更低一级上，是三者中最慢的 768.8 µs。',
  ],
  quiz: [
    {
      q: '这一帧在最窄的一档要 129.6 µs；到最宽的一档，子载波是八倍，却仍要 61.6 µs。为什么不是八分之一？',
      options: [
        '数据帧变短，ACK 就会变长',
        '数据前面有一段固定的前导，而数据本身最短也不能少于一个完整符号',
        '宽信道的发射功率更低',
      ],
      answer: 1,
      explain: '按比例缩短的只有数据部分：81.6 µs 的符号最后只剩一个 13.6 µs 的符号。48 µs 的前导纹丝不动，此时已占了整帧四分之三以上。',
    },
    {
      q: '手机在覆盖边缘。把它从最宽的信道换到窄一些的，是更快还是更慢？',
      options: [
        '更慢——子载波只剩四分之一，速度也只剩四分之一',
        '通常更快：信号已经勉强时，窄信道换回来的灵敏度，比让出去的那些子载波更值钱',
        '没区别——带宽不影响覆盖',
      ],
      answer: 1,
      explain: '在这套房子最远的角落里，160 MHz 一帧也送不到，而 20、40、80 MHz 依然能把帧送出去。这三者谁更快，是另一个问题。',
    },
  ],
}
