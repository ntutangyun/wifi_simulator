/**
 * Wi-Fi Tier 2 · M9 · 容量旋钮与速率控制 · The bill an excursion runs up.
 *
 * The second half of `rate-fallback`, split on 2026-09-25
 * (docs/superpowers/plans/2026-09-25-course-repacing-proposal.md, §2 M9):
 * 跌落的账单——20.2% 的帧吃掉 29.7% 的空口时间，邻居也在付. `rate-fallback` owns
 * the rule; this lesson owns what a stretch below the ceiling costs, and who
 * pays it.
 *
 * What arrived with the prose: 「账单落在邻居头上」, the 20.2 %/29.7 % arithmetic,
 * the freeze table and the excursion tax. §5.3 names the two paragraphs that
 * printed 530.3/318.1/212.2 ms back to back, so those figures are stated once
 * here — in the first table — and the sentence after it carries no numbers at
 * all. §4 gives this lesson no diagram: 「两张表」 is the whole of it.
 *
 * It loads `rate-fallback`'s scene (which is `rate`'s) with no variant, so its
 * line in tests/fixtures/lesson-hashes.json is a copy: the split costs the suite
 * no extra simulation. Every number quoted below is pinned in
 * tests/course/rate-cost.test.ts.
 */
import { type Lesson, firstData, firstFreeze, J } from '../lessonKit'
import { rateScenario } from '../wifiScenes'

export const rateCost: Lesson = {
  id: 'rate-cost',
  module: 8,
  title: '跌落的账单——谁替这段慢帧买单',
  why: '上一课的规则会把一台站点（STA）压在它的上限之下待上一段。那段时间里它并没有少发东西，只是每一帧都更长；而信道是一个人说话、所有人听着的地方，所以这段多出来的时间，并不只是它自己的损失。这一课把这笔账算出来：跌落中的帧占多少、吃掉多少空口时间（airtime），以及一台什么也没做错的邻居为此多等了多久。',
  outcomes: [
    '量出跑在上限之下的帧占多少帧、占多少空口时间',
    '说清一台邻居为什么要替别人的跌落多等，并读出它被挡住多久',
    '说出这笔多花的时间换来了什么——什么也没有',
  ],
  needs: ['rate-fallback'],
  terms: [
    { term: 'airtime tax', plain: '同样的内容因为跑在更低一级而多占住的那段空口时间；它没有多送出一个比特' },
    { term: 'backoff freeze', plain: '介质一忙，别人的退避计数器就原地停住——退避那一课的规则；这一课量的是它停了多久' },
  ],
  picture: [
    { heading: '一帧变长，代价是两份', text: '跌落一级，这一帧的载荷（payload）一个字节都没变，只是每一块信号装的比特少了，于是它在空口上待得更久。第一份代价是它自己的：同样的三秒里，它能交付的帧更少。第二份代价不在它身上——它多占住信道的每一微秒，都是屋里其他人不能开口的一微秒。' },
    { kind: 'watch', jump: 1, heading: '去看一眼', text: '载入仿真，跳到第一次退避（backoff）冻结：正在倒数的那台站点停住了，因为另一台开始发了。然后在远端站点那条泳道上找一个最长的方块，看近端站点脚下那段冻结有多长——它比那一帧本身还长一截。' },
    { heading: '账单落在邻居头上', text: '跑在更低一级调制与编码方式（modulation and coding scheme, MCS）的站点，为同样的内容要占住信道更久；而信道一忙，其他人的退避计数器就原地冻结。所以一次跌落的代价不只由跌下去的那台站点承担：每一个邻居，为自己早就赢到的那次机会，都要多等。' },
    { kind: 'steps', heading: '这笔钱是怎么落到邻居头上的，一步一步', items: [
      '远端站点在低一级上发出一帧：同样的 1530 字节，只是这一帧更长——上限上 524.0 µs，降一级 768.8 µs，最底一级 1 476.0 µs。',
      '近端站点此刻正在倒数。介质（medium）一被判为忙，它的退避计数器就立刻停住，并把当前值记下来（引擎里的 `onCcaBusy`；§10.23.2.4）。',
      '远端那一帧发完，还要留出它的答复；直到介质重新空闲、近端站点又等满自己该等的那一段，计数器才继续——而且是从进去时的那个值继续，一格不多，一格不少。',
      '所以这台邻居被挡住的，是从冻结到恢复的那一整段，而不是那一帧的空口时间：上限上 615.0 µs，最底一级 1 579.0 µs。',
      '把远端站点跑在上限之下的每一帧都这么算一遍，就是下面这两张表。',
    ] },
  ],
  numbers: [
    { kind: 'table', heading: '远端站点的三秒：跌落中的那些帧，和它们的账单', head: [
      '项目', '数值',
    ], rows: [
      ['跑在上限之下的帧数', '607'],
      ['占它三秒里发出的帧数', '20.2%'],
      ['占它三秒里的空口时间', '29.7%'],
      ['这些帧实际花掉的空口时间', '530.3 ms'],
      ['同样这些帧若跑在上限上', '318.1 ms'],
      ['于是多烧掉', '212.2 ms'],
      ['占整段三秒仿真', '7.1%'],
      ['本次最短的一趟：最底一级上的十帧', '14.8 ms，而在上限上只要 5.2 ms'],
    ] },
    { kind: 'table', heading: '近端站点的退避，被一个远端站点的帧冻结多久——从冻住算到恢复的那个时隙（slot time），而不是那帧的空口时间', head: [
      '那一帧的级别', '退避被冻结',
    ], rows: [
      ['MCS 2——上限', '615.0 µs'],
      ['MCS 1', '859.8 µs'],
      ['MCS 0——最底一级', '1,579.0 µs'],
      ['从上限到最底一级：每帧多等', '964.0 µs'],
    ] },
    { heading: '这笔钱买到了什么', text: '什么也没有。帧数一样，载荷一样，送到的内容一个字节不差——多出来的只是空口上那一段，而这一段是屋里所有人一起等的。这也正是速率异常又一次出现：只不过这一次，它是发送端能自己爬出来的一种状态，而不是距离带来的固定事实。' },
  ],
  deeper: [
    { heading: '为什么不是三倍', text: '最底一级的一帧几乎是上限上的三倍长（1 476.0 对 524.0 µs），可这 607 帧总共只花了 1.67 倍的时间。原因是跌落大多只掉一级：607 帧里有 517 帧在 MCS 1 上，每帧多 47%；只有 90 帧掉到了最底一级，那里每帧多 182%。“多数跌落只掉一级”是这笔账没有更难看的唯一理由——而它成立，靠的是规则里那个“连续两次”：上一课数过，25 次跌落里只有 6 次深到最底一级。' },
    { heading: '这笔账里没有算进去的东西', text: '上面两张表只算了空口上的时间。真实系统里还要接着算下去：邻居多等的那段时间会让它的队列变长，队列一长，时延与抖动就跟着变；而在一间有语音或视频的房子里，抖动比吞吐更难交待。本仿真器把这些留给了另一条线——队列那一课量的是等待，这一课量的是等待从哪儿来的。' },
  ],
  sources: [
    '“介质忙时冻结”见 IEEE Std 802.11-2024 §10.23.2.4：介质被判为忙时退避计数器挂起，恢复时沿用它原来的值。本课表里那三个冻结时长是本仿真器的记录之差——从 BACKOFF_FREEZE 到 BACKOFF_RESUME——所以它们里面已经含了帧尾之后的答复与那一段静默。',
    '三个级别的空口时间来自 §17.4.3 的 TXTIME 公式，代入的是本仿真器为这类 PPDU 选定的代表性前导与符号长度；“多 47%”“多 182%”是这三个时长之比，不是标准里的任何数字。',
    '速率异常（performance anomaly）出自 Heusse 等人 2003 年的 INFOCOM 论文，标准并没有这个概念；标准只规定每一级怎么发，不规定谁该用哪一级。',
  ],
  scenario: () => rateScenario(),
  jumps: [
    J('第一个数据帧', firstData),
    J('第一次退避冻结', firstFreeze),
    J('第一次 ACK 超时', (r) => r.type === 'ACK_TIMEOUT'),
  ],
  observe: [
    '把两条泳道并排看：远端站点每落一级，近端站点脚下那段冻结就更长——615.0、859.8、1,579.0 µs。它自己一帧未丢、级别一动不动，多等的时间却全由邻居的运气决定。',
    '远端站点跑在上限之下的帧只占五分之一，却占掉将近三成的空口时间。把这条泳道整段拉完，能看见这些更长的方块是怎么一簇一簇出现的：一次跌落至少要十帧才爬得回来。',
  ],
  tryThis: [
    '点“在编辑器中打开”，把近端站点的流量从饱和改成空闲。远端站点再没有对手，337 次失败一次也不剩：它三秒里发 4,395 帧，帧帧都在上限上，这笔账归零。开出这张账单的是竞争，不是距离。',
    '把近端站点放回去，改成把远端站点朝接入点（access point, AP）挪两米。上限抬到 MCS 3，三秒交付的帧数从 3,003 变成 3,712，而多烧掉的时间从 212.2 ms 降到 96.9 ms——占仿真的 3.2%。房间宽裕了，但只要还有人竞争，账单就不会归零。',
  ],
  quiz: [
    {
      q: '远端站点在最底一级上发一帧，把近端站点的退避计数器冻住了多久？',
      options: [
        '1,476.0 µs——就是那一帧的空口时间',
        '1,579.0 µs——从冻住那一刻，到它能继续倒数的那个时隙',
        '一格也没冻住：退避只受自己的计数器控制',
      ],
      answer: 1,
      explain: '冻结从介质被判为忙开始，到介质重新空闲、又等满该等的那一段之后才解除，所以它比那一帧本身长一截。同一台邻居在上限上的那些帧后面只等 615.0 µs。',
    },
    {
      q: '跌落中的帧只占远端站点帧数的 20.2%，为什么吃掉 29.7% 的空口时间？',
      options: [
        '因为它们更容易被撞，要多发好几次',
        '因为同样的载荷在更低一级上要更长：517 帧每帧多 47%，90 帧每帧多 182%',
        '因为它们排得更靠后，等得更久',
      ],
      answer: 1,
      explain: '空口时间跟级别走，不跟失败次数走：每一帧的载荷一个字节都没变。加起来这 607 帧花了 530.3 ms，而它们若都在上限上只要 318.1 ms。',
    },
  ],
}
