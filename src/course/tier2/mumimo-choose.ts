/**
 * Wi-Fi Tier 2 · M10 · Scheduled Wi-Fi 6/7 · which of the two ways this turn takes.
 *
 * The second half of `mumimo`
 * (docs/superpowers/plans/2026-09-25-course-repacing-proposal.md, §2 M10): the
 * first half is what dividing space means and how big a group the router's
 * antennas allow; this one is the other rule the engine follows — the load
 * threshold it picks by — and what each way actually buys, side by side.
 *
 * §4 gives this lesson NO diagram: the comparison table IS the lesson.
 *
 * It loads `mumimo`'s own scene and its two variants unchanged (`sameSceneAs`),
 * so the recorded timeline hashes are the same run twice and the fixture lines
 * the controller adds are copies. Every number is pinned in
 * tests/course/mumimo-choose.test.ts.
 */
import { type Lesson, firstData, firstBa, firstMuDl, J } from '../lessonKit'
import { mumimoScenario } from '../wifiScenes'

export const mumimoChoose: Lesson = {
  id: 'mumimo-choose',
  module: 9,
  title: '按频率还是按空间：这一轮怎么选',
  why: '同一栋房子里，路由器有两种把好几部手机塞进一次发送的办法：按频率切成片，或者按空间瞄准。两种办法买到的东西正好相反——一种让一趟运得更多，一种让每个成员更快拿到自己的东西——而这一轮到底走哪一条，引擎有一条写死的判据。这一课把两种发送各取一次并排放着，再把那条判据读出来。',
  outcomes: [
    '说出按空间划分什么时候划算、按频率划分什么时候划算，并各给一个理由',
    '读出引擎挑法子时看的那个量，以及它为什么定在那里',
    '在两个变体里对照同一份负载的数据段有多长，以及整帧为什么只差 1.41 倍',
  ],
  needs: ['mumimo'],
  terms: [
    { term: 'symbol', plain: '符号：一次发送里数据段的最小时间单位，本仿真器为 Wi-Fi 7 帧取 13.6 µs' },
    { term: 'MU', plain: '多用户：形容一次发送里的各份分别属于不同的设备' },
    { term: 'OFDMA', plain: '正交频分多址：按频率划分的那个办法，把一条信道切成几片分给几个成员' },
  ],
  picture: [
    { heading: '两种办法，两笔正好相反的账', text: '按空间划分——也就是多用户 MIMO（multi-user MIMO, MU-MIMO）——是把速率乘上去：成员独占整条信道，同一份负载（payload）要的符号（symbol）就少。这一乘只有在数据符号本来就够多时才划算。按频率划分——正交频分多址（orthogonal frequency-division multiple access, OFDMA）——是把开头那段前导码（preamble）摊薄：几个成员合用一段前导码，而在数据符号本来就没几个时，这一摊最划算。所以许多小帧该去分片，少数几个大帧该去分波束——而那段谁也缩不短的前导码，则在背后默默稀释着两边各自赢来的东西。' },
    { kind: 'watch', jump: 0, heading: '去看一眼', text: '载入仿真，跳到第一个同时装着多部手机的发送，然后在时间轴上方的两个变体之间来回切换：同一栋房子、同样的流量、同样的手机——只是那个宽块在一边装着三份，在另一边只装两份。把鼠标停在上面，读出每一份的字节数和速率。' },
    { heading: '引擎按什么挑', text: '判据不是“哪个更快”，而是一个能当场算出来的量：每个候选队首排着的那一帧有多大。低于 1,000 字节，它就按频率划分；每个候选都够大，才按空间划分。理由就在上面那笔账里：帧太小，数据符号本来就只有一两个，把速率乘上去省不出什么，而前导码照付；帧够大，符号数才多到值得去乘。这个门槛是引擎自己定的一个数，不是标准里的规定。' },
  ],
  numbers: [
    { kind: 'table', heading: '两种发送各取一次，都来自这栋房子的两个变体——160 MHz，每部手机两条流', head: [
      '变体', '成员数', '每成员负载',
      '数据符号', '整帧长度', '单成员速率',
    ], rows: [
      ['按频率划分', '3', '4,306 B', '3', '92.8 µs', '371.2 Mb/s'],
      ['按空间划分', '2', '4,306 B', '1', '65.6 µs', '525.1 Mb/s'],
    ] },
    { kind: 'formula', heading: '一次发送的长度是怎么来的', text: '整帧长度 = 52 µs + 13.6 µs × 数据符号数', note: '这里的 52 µs，是 Wi-Fi 7 帧的 48 µs 前导码加上 4 µs 的“这一发里有谁”分配表。按频率划分的成员只占三分之一子载波（sub-carrier），同样的负载要三个数据符号；按空间划分的成员独占整条信道，只要一个——干净的三倍增益，正好等于它放弃掉的那个组的大小。可整帧算下来只差 1.41 倍，因为前导码那一段从不缩短。' },
    { heading: '合起来交付的量，方向正相反', text: '两种发送里，每个成员的负载是一样的——都是路由器给那部手机攒下的三个视频帧。三个成员一次交付 12,918 字节，两个成员是 8,612 字节：成员多的那次一趟运得更多，成员少的那次则让每个成员更快拿到自己的东西。你要哪一个，取决于有没有人在等。' },
    { kind: 'steps', heading: '这一轮走哪一条，一步一步', items: [
      '路由器赢下一轮，先按上一课的办法把名单列出来：队列（queue）里有东西、协商过多用户（multi-user, MU）发送，最多四部。这一份名单两条路共用。',
      '逐个检查候选：每一部都协商过 MU-MIMO 吗？每一部队首那一帧都有 1,000 字节吗？有任何一项不成立，这一轮就按频率划分。',
      '都成立，就按空间划分——再按上一课那道天线（antenna）上限把组裁到装得下；裁完不足两部，仍旧退回按频率划分。',
      '然后为每个成员数符号数：⌈(16 + 8 × 字节数 + 6) ÷ 每符号比特数⌉。按频率划分时，每符号比特数要乘上它分到的子载波占比；按空间划分时不乘，但每部仍按自己协商的流数算。',
      '整次发送的长度取最长的那个成员所需：48 µs 前导码，加 4 µs 分配表，然后每个符号 13.6 µs。两条路上，4,306 字节分别要三个符号和一个符号。',
    ] },
    { kind: 'table', heading: '同一份 4,306 字节，两条路各算一遍', head: [
      '步骤', '按频率划分', '按空间划分',
    ], rows: [
      ['队列里有东西的手机', '3', '3'],
      ['都协商过 MU-MIMO，且队首都有 1,000 字节',
        '否', '是'],
      ['裁完后的成员数', '3', '2'],
      ['每个成员拿到的子载波', '三分之一', '全部'],
      ['每个成员每符号能驮的比特', '三分之一份', '一整份'],
      ['每个成员的符号数', '3', '1'],
      ['于是这次发送的长度是', '52 + 13.6 × 3 = 92.8 µs', '52 + 13.6 × 1 = 65.6 µs'],
      ['这一次合起来交付', '12,918 B', '8,612 B'],
    ] },
  ],
  deeper: [
    { heading: '为什么门槛管的是队首那一帧，而不是整条队列', text: '引擎问的是每个候选队首那一帧有多大，不是它一共积压了多少。这与长度是怎么定出来的相吻合：一次发送的长度由最长的成员决定，而每个成员能往自己那一份里装多少，是一帧一帧往里填、填到装不下为止。队首那一帧要是很小，这个成员的符号数就少，把速率乘上去也省不出什么——即便它后面还排着一百帧。' },
  ],
  sources: [
    '两种多用户发送的 PPDU 格式与每用户字段，Wi-Fi 6 见 IEEE Std 802.11-2024 第 27 章，Wi-Fi 7 见第 36 章。标准并不规定发射机该在两者之间怎么选：那是厂商的调度策略。',
    '1,000 字节的门槛、一组最多四个成员，都是本引擎自己的规定（mac.ts 里的 `MUMIMO_MIN_BYTES` 与 `muDsts.slice(0, 4)`），是模型取值。',
    '48 µs 前导码、多用户信令多出的 4 µs 以及 13.6 µs 的符号，是本仿真器为 802.11be PPDU 取的代表值，并非逐字段相加；它们代入的空口时间公式是 §17.4.3 的 TXTIME。',
  ],
  scenario: () => mumimoScenario(false),
  variants: [
    { label: 'OFDMA（按频率划分）', scenario: () => mumimoScenario(false) },
    { label: 'MU-MIMO（按空间划分）', scenario: () => mumimoScenario(true) },
  ],
  jumps: [
    J('第一次多用户发送', firstMuDl),
    J('第一个数据帧', firstData),
    J('第一个 BlockAck', firstBa),
  ],
  observe: [
    '第一个变体里，宽蓝块装三份，每份各占三分之一条信道，单成员速率 371.2 Mb/s；第二个变体里，同样的块只装两份，各自独占整条信道，525.1 Mb/s。同一份 4,306 字节，一边要三个数据符号，一边只要一个。',
    '两个变体里，那个宽块的长度差得远没有速率差得多：92.8 µs 对 65.6 µs，只有 1.41 倍。差出来的三倍全在数据段上，而前导码和分配表两边一样长。',
  ],
  tryThis: [
    '关掉笔记本的备份流量再重新载入。路由器现在能在下一个视频包到达之前就把当前这部手机的包送完，于是两种多用户发送都变得罕见——已经没有什么可以凑成一组了。这也说明了判据里那个量从哪儿来：凑得起组，靠的是有人在等。',
  ],
  quiz: [
    {
      q: '三部手机，一次发送。在按频率划分的办法下，再加入第四部之后，每部手机的速率会怎样？',
      options: [
        '下降——信道又被多分了一份',
        '不变——每个成员都拿到整条信道',
        '上升——成员越多，每人分摊的开销越少',
      ],
      answer: 0,
      explain: '按频率划分的成员分享的是子载波，而不是时间：多一个成员，每个人的那一片都更薄。这正是按空间划分不肯付的代价，它改用天线来付。',
    },
    {
      q: '路由器这一轮为什么按频率划分，而不是按空间划分？',
      options: [
        '因为按空间划分总是更慢',
        '因为至少有一部候选手机的队首那一帧不到 1,000 字节，或者有一部没协商过 MU-MIMO',
        '因为房间里手机太多了',
      ],
      answer: 1,
      explain: '判据是当场能算出来的两件事：都协商过吗，队首都够大吗。帧太小，符号数本来就少，把速率乘上去省不出什么，而前导码照付。',
    },
  ],
}
