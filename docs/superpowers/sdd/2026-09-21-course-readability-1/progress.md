# SDD ledger — plan: docs/superpowers/plans/2026-09-21-course-readability-1.md

Worktree: D:\wifi_sim\.claude\worktrees\feat-link-2g (branch feat/uwb-ranging). Spec: docs/superpowers/specs/2026-09-21-course-readability-design.md.
Context: user feedback 2026-09-21 — lessons too hard to follow; revise the course before more features. The AMP backscatter slice (ledger 2026-09-21-amp-backscatter) is paused after its Task 3/4; its lesson (T5) is written under this contract later.

## Pre-flight scan

| Pair / task | Produces vs consumes | Finding |
|---|---|---|
| T1 → T2/T3 | `Term`, `watch` block, optional new `Lesson` fields, `isMigrated`, `lessonBlocks`, `trackOf`, `MIGRATING`, `paragraphTexts`, i18n section labels | consistent; T2/T3 only remove their id from `MIGRATING` and add one id to `COURSE_ORDER` |
| T2 ↔ T3 | both edit `curriculum.ts` COURSE_ORDER, `lessons.ts`, `readability.test.ts` MIGRATING, `lesson-hashes.json` | serialise (T2 then T3) — never concurrent |
| T1 self | the rule test's citation case includes a self-contradicting comment line about "draft" — the case as written replaces "draft" with "drafty" before asserting, so it is consistent; implementer may simplify the string | ruling: simplify to plain negative cases; the assertion set must stay |
| T1 self | `describe.each` over an empty list | guard with `if (migrated.length)` as the plan allows |
| T1 ↔ existing tests | `tests/course/*.test.ts` read `.body` on old-shape lessons — still valid (field optional) | no change needed until each lesson migrates |
| T2/T3 ↔ pins | moving describes between test files must not drop a pin | reviewer checks the moved-test inventory against the plan's lists |
| T2/T3 ↔ fixture | additions only, hashes equal to the first half's | guarded by the plan's Step 4 |
| T1 ↔ AMP Task 3 | both touch `src/ui/i18n.ts` (course strings vs record labels) | ruling: do not dispatch T1 until AMP T3 has committed |
| Global | AMP-track prerequisite rule: module 7 is pseudo-track 'amp' | `trackOf` in T1 |

Model plan: T1 opus (renderer + test design), T2 opus, T3 opus; reviewers opus; novice-read reviewer sonnet (reads as a beginner; judgment on prose is what matters, cheap model fits the persona) then opus task review.

## Progress
Task 1: dispatched (base 70f44de, opus)
Task 1: 926e10e DONE_WITH_CONCERNS (CITATION regex needed `(?:r\d+)?` to match 11-24/1613r20 — accepted; 48 `.body!` edits in per-lesson tests until step 7; `watch` added to the block-shape switch; renderer unseen in a browser — controller checks with the dev server after T2; `trackOf` keys off module 7 literal — carry: derive from MODULES title or add a `track` field when AMP migrates). Reviewer dispatched (opus). Task 2 dispatched concurrently (base 926e10e, opus) — owns src/course/uwb/uwb-intro.ts, uwb-frame.ts, curriculum.ts COURSE_ORDER, lessons.ts, tests/course/uwb-intro|uwb-frame|readability tests, lesson-hashes.json.
Task 1 review: spec ✅ / quality approved — 1 important (paragraphTexts drops list/steps items and headings, so citations/acronyms/caps in list items go unchecked), 9 minor. Ruling: list/steps items and block headings ARE checked — extend `paragraphTexts` in readability.ts (the test already routes through it), caps apply to items in `picture`; the change is confined to readability.ts + readability-rules.test.ts because T2 is concurrently editing readability.test.ts; T2 told to write to the stricter rule. Minors that need readability.test.ts wait for the whole-branch fix wave. Fix round 1: resume implementer (opus).
Task 1 fix round 1: 6bc18be (paragraphTexts covers headings + list/steps items; minors 3,4,5,8, 7a). Ruling on minor 2: acronym tokens have NO upper length bound — the spec's "2–6" was wrong (RMARKER must be caught); spec amended. Deferred to the fix wave (readability.test.ts owned by T2): #6 ZH-side acronym/quantity checks, #7b bilingual walk over sources/outcomes/terms.plain, #9 firstOfTrack comment, i18n `course.why` key (spec lists it; add or drop from spec at step 7). `.body!` collateral is 36 sites / 14 files. Re-review dispatched (sonnet).
Task 1 re-review: ALL ADDRESSED-OR-DEFERRED
Task 1: complete (926e10e + 6bc18be; spec wording fc5195d)
Task 2: 46ee066 DONE_WITH_CONCERNS. Rulings: uwb-record-hashes.json additions (two lines equal to uwb-intro's) accepted — the fixture rule is "additions only" for a new id sharing a scene; the plan's per-lesson word windows were narrower than lessonWords' scope — the spec's 500–1300 / ≤ 20 min binds, plan windows advisory; "PSDU is the smallest part" was a plan error — corrected wording accepted. Novice-read reviewer (sonnet) and task reviewer (opus) dispatched in parallel; controller renders both lessons in the browser (dev server + Playwright).
Browser check (controller, Playwright, ZH): uwb-intro renders in order why (13.5px) → outcomes → needs buttons (titles) → terms table → picture with two watch call-outs (3px accent, load button) → numbers → deeper <details> closed → load/variants → observe/experiments/quiz → sources <details> closed; watch button loads the sim and turns into "⚡ 跳到那里"; the jump list appears. Panel 324px wide, no overflow. PASS.
Task 2 novice read: uwb-intro READY (polish: define "chip"; EN "sigma" → "standard deviation"; one overloaded formula note); uwb-frame NEEDS A PASS (stop point: observe item says "read right to left … PSDU comes last" — contradiction; symbol vs chip units unreconciled; "chip" undefined). Held for the task review to combine into one fix round.
Task 2 review: spec ❌ / needs changes — 1 important (uwb-frame "everything after the RMARKER is the message" is false: STS gaps, STS, PHR sit between; plus no pin for "first watch within three picture blocks"), 8 minor. Fix round 1 (combined with the novice read): resume implementer (opus).
Task 2 fix round 1: fe99177 (frame order truthful and pinned; right-to-left slip fixed; chip glossed / in uwb-frame terms; symbol vs chip reconciled with pinned counts; formula note split; first-watch pin). Declines accepted: no pin for the wall/hand sentence (no numeric claim); UWB/anchor glossed in why+terms; "sigma" glossed not replaced (uwb-position test asserts the literal until it migrates); Units "where" column kept, shortened. Re-review dispatched (sonnet). Task 3 dispatched concurrently (base fe99177, opus).
Task 2 re-review: ALL ADDRESSED (nit: SFD row of the "Two words, two sizes" table does not restate 508 — carry to the fix wave)
Task 2: complete (46ee066 + fe99177)
Task 3: d7078c7 DONE_WITH_CONCERNS (three numbers items moved to deeper to fit 1300 — accepted, no quiz depends on them; amp-coexist "前两课" now stale — carry to the AMP step; 15/16 words of headroom). Novice-read (sonnet) and task review (opus) dispatched in parallel.
Task 3 novice read: amp-intro READY (polish: three record names unexplained; ACWE/ACW formula before the plain idea); amp-ppdu NEEDS A PASS (U-SIG / legacy signal field never explained; "closing extension" used before defined; a contribution citation inside the main table). Held for the task review to combine into one fix round.
Task 3 review: spec ✅ / needs fixes — 2 important (AMP-Sync / AMP-SIG never spelled out in picture; the bilingual walk duplicated a fourth time — ruling: extract one exported helper now that tests/course is free, and use it in all four lesson tests + readability.test.ts), 6 minor. Fix round 1 (combined with the novice read; also picks up T1's deferred #6 ZH-side checks, #7b bilingual walk over sources/outcomes/terms.plain, #9 firstOfTrack comment): resume implementer (opus).
Ruling: `course.why` i18n key dropped from the spec (the opening has no label by design).
Task 3 fix round 1: 070f21a (AMP-Sync/AMP-SIG/U-SIG in plain words; citation out of the table; record names glossed; lessonStrings() helper shared by four lesson tests + readability; ZH-side acronym/quantity checks; bilingual walk over sources/outcomes/terms; minors). Declines accepted: module label is M8 (0-based MODULES); lighter ZH reordering kept; no split of "A second of polling"; shared test fixtures across lesson tests carried to a later step. Re-review dispatched (sonnet).
Whole-branch review of step 1 dispatched (fable, head 070f21a) — asked for spec/plan amendments for steps 2–7 as the main product.
Task 3 re-review: ALL ADDRESSED (new minor: ZH typo 掀不准 → 掐不准 — fixed by the controller directly)
Task 3: complete (d7078c7 + 070f21a)
Whole-branch review (fable): NEEDS A FIX WAVE — 5 important (I1 amp-intro observe quotes the Door tag's line for the Fridge tag; I2 acronym admission narrower than spec + TIER1_BASELINE hides NAV/CTS until step 5; I3 courseLoaded global → watch "Jump there" into another lesson's recording; I4 deeper unchecked for citations; I5 amp-intro "Where the losses come from" 100 w / 13 q), 13 minor, 12 amendments. Rulings: A1–A8 ADOPTED into the spec now (Tier 1 admission; numbers-prose caps 90/170/4 q + acronym rule; term-density cap; section budgets 650/350/400, first lesson ≤ 1000; lessonWords counts N() cells and formula bodies as one word; observe/try caps 60 w / 6 q + pin against the named record; deeper in the citation check; word-quantities agree across languages); A9–A12 written into the spec as "Plan template for steps 2–7" (kit.ts lessonShapeSuite + shared fixtures, prose windows, fixture wording, .body! retirement, batch by scene, lesson-dump script, courseLoadedFor, step-5 baseline table). ONE fix dispatch (opus, fresh): I1–I5, the test changes for A2/A3/A4/A5/A6/A7, courseLoadedFor, and re-trimming the four lessons to the new budgets (openers ≤ 1000).
Fix wave: 5d04baa — openers 994 / 998 words, all budgets met, courseLoadedFor, lesson-dump script; carries M6, M10 (docs), M12 (kit.ts → step 2 T1), M13 (step 5), div onClick a11y. Controller ran the gates (1966 tests, tsc, build green) and merged without a further re-review round to shorten the loop (user asked why so slow); the step-2 whole-step review covers any residue.
Step 1: COMPLETE — merged to main.
