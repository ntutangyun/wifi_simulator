# SDD ledger — plan: docs/superpowers/plans/2026-09-23-mechanism-before-metaphor.md

Worktree D:\wifi_sim\.claude\worktrees\feat-link-2g (feat/uwb-ranging). Spec: the 2026-09-23 amendment in docs/superpowers/specs/2026-09-21-course-readability-design.md. User 2026-09-23: revise ALL courses in this style; AMP still paused.

Pre-flight: no task pair shares a file (one file per lesson, one test per lesson); the controller owns MECHANISM_DONE, readability.ts, curriculum.ts, lessons.ts and both fixtures. Reference lessons landed before the batches: decode-thresholds (procedure) c395e14, roles-stack (naming) this wave.

Rule inventory at the start, all 45 unrevised lessons graded with MECHANISM_INCLUDE: 37 lack a procedure block, 24 fail the stand-in naming rule (EN masks ZH in the same `it`, so that count is a floor), 4 carry 这笔账, 1 uses 余量 unglossed (radio-primer).

## Progress
Rules + reference lessons: c395e14 (decode-thresholds, procedure), de14765 (naming rule + roles-stack). MECHANISM_DONE = decode-thresholds, roles-stack.
Wave 1 dispatched (opus, base de14765): B1 (radio-primer, frame-anatomy, frame-anatomy-bytes) ∥ B2 (airtime, ifs, backoff, nav) ∥ B3 (hidden, anomaly, retries-queues).
B2: 6041b35 DONE (airtime 1258, ifs 1393, backoff 1294, nav 1249; ifs's "One ladder, three rungs" was a steps block that only ranked the gaps — demoted to a list and the real access procedure written as six steps in numbers: ruling ACCEPTED, a steps block must be the engine's procedure, not a ranking; nav cannot name CF-End/RTS early release because RTS/CTS belong to the later `hidden` — carry to the track review; engine added four facts the prose had omitted, now written in). B2 review dispatched (sonnet).
B2 review: NEEDS FIXES — 1 important (nav's freeze table ends "A resumes at 3" with 3 never defined: it is A's frozen backoff counter — the unglossed-quantity defect itself), 0 minor. All four steps blocks verified against the engine's own order and constants (txTimeModeNs; startAccessAc/beginIfsAc/onIfsEndAc; the CW/rng.int/bumpQsrc chain; updateNav's later-of rule); no pin lost. Fix round 1: B2 implementer resumed.
C1 dispatched (opus, base 6041b35): edca, ampdu, txop, txop-protect.
B2 fix round 1: 70ed2be (nav's frozen 3 glossed where the table prints it, new pin proving the gloss: A's next frame starts exactly 3 slot times after the resume, the three BACKOFF_DEC records read 2,1,0; nav 1269 words). B2: complete.
INCIDENT: this worktree has one shared git index; B2's bare `git commit` swept B1's already-staged six files into 70ed2be under a nav message. Nothing lost — B1's content is committed as staged. No amend, no reset (the brief forbids both and they would destroy other agents' commits). B1 told; brief now requires `git commit -F - -- <paths>` so a commit can never carry another batch's index.
B1: no commit of its own — its six files were the ones swept into 70ed2be, content intact (radio-primer 993/1000 with the link budget as six steps; frame-anatomy 1552 with buildDataFrame/dataMpdu as six steps + a worked table; frame-anatomy-bytes 1508 with txTimeModeNs as six steps, 这笔账 replaced by the count). Ruling ACCEPTED: radio-primer stops using 余量 rather than spending the opener's last of four terms on a synonym of SNR — `rate margin` stays in decode-thresholds; its SNR gloss gained 噪声地板. Carries: radio-primer is 7 words under the opener ceiling (any future addition forces a split); proseMax is a per-lesson argument now set to 1200 in three lesson suites — centralise it at the track review.
RULE BUG (found by B1, fixed by the controller at ef74e20): `namedInPlace` built its regex in a template literal, where `\b` is the backspace character — the terms half of rule 4 passed every lesson vacuously from the moment it was written. Fixed with three honest allowances (naming clause "is the X"/"are X"; an unclosed bracket up to 30 chars back; a gloss that FOLLOWS the name). roles-stack fixed too (MAC/PHY were named and glossed nowhere).
Re-graded the two landed batches with the working rule: six real gaps (frame-anatomy FCS zh, frame-anatomy-bytes L-STF, airtime MAC, ifs DIFS, backoff backoff, nav NAV). One fix dispatch (opus) covers all seven B1+B2 lessons.
B3: 880f9c7 DONE (hidden 1513, anomaly 1391, retries-queues 1546; no splits, no new terms). Two engine-over-prose corrections, both pinned: anomaly — a station waits EIFS, not DIFS, after a reception it could not decode (166 of 833 waits, all at the far station); retries-queues — purgeExpired discards ANY queued MSDU past its lifetime, attempts included (6 of the AP's 194, 9 of the stations' 11; 188 of 194 never had a turn), so "without ever sending it" was wrong. B3 review dispatched (sonnet).
C2 dispatched (opus, base 880f9c7): width, streams, rate, rate-fallback.
B3 review: PASS (0 important, 0 minor). The reviewer re-derived both corrections from the engine with its own scripts rather than trusting the report. Registered 0d65220 — MECHANISM_DONE = decode-thresholds, roles-stack, hidden, anomaly, retries-queues. B3: complete.
C3 dispatched (opus, base 0d65220): ofdma-dl, ofdma-ul, mumimo.
Naming fix round (B1+B2's seven lessons): b23e991. The six reported gaps were only the first failure per lesson — the assertion short-circuited — and eleven first uses needed fixing once the first six were cleared (frame-anatomy MAC/PHY/FCS/CRC/QoS, frame-anatomy-bytes L-STF/L-LTF/L-SIG/U-SIG, airtime MAC, ifs DIFS, backoff backoff+CW, nav NAV; radio-primer needed nothing). RULE FIX 9b1c187: the naming test now collects every failure in a lesson — both languages, terms, borrowed words, both stand-ins — and asserts the list is empty, so one run tells an author everything the lesson owes. Registered airtime/ifs/backoff/nav (B2 reviewed clean). Carries: frame-anatomy now sits at 1199/1200 prose and 539/550 numbers; proseMax is a per-lesson argument set by hand in six lesson suites — centralise at the track review.
B1 review dispatched (sonnet).
B1 review: PASS (0 important, 2 minor — a generality wedged between steps 4 and 6 of frame-anatomy-bytes' worked count; a leftover self-review comment in tests/course/frame-anatomy.test.ts; both to the track fix wave). Reviewer re-derived all four worked examples from the engine (12.16 dB SINR, Duration 44 µs, 1528 B, 57 symbols/248 µs) — all exact. Registered dc6a280. B1: complete. Twelve lessons on the amendment.
C4 dispatched (opus, base dc6a280): mlo, capstone.
C1: a7e9495 DONE (edca 1543, ampdu 1341, txop 1385, txop-protect 1539). Engine over prose: ampdu's steps now state the real rule — whole-PPDU decode, so a timeout counts a retry on every subframe and doubles the window; there is no per-subframe bitmap in this engine.
RULE CONFLICT found by C1 and ruled on by the controller (a090ebf): rule 4 wants 接入点（AP）; the Tier 2 vocabulary sheet banned "AP" in Chinese outright. The ban was written against a bare AP in Chinese prose; a name bracketed onto its Chinese word is the opposite of that, so 接入点（AP） is allowed once, at the word's introduction, and a bare AP is still rejected. C1's workaround (txop/txop-protect stopped saying 接入点 in the picture at all, leaving a seam between picture and numbers) is being undone in a follow-up round.
C3: 9626f11 DONE (ofdma-dl 1412, ofdma-ul 1462, mumimo 1406). Three engine findings, all pinned: transmitTrigger does NOT apply the trigger frame's per-device power correction (standard text, not simulated — stated plainly in deeper, "the round you watch is the easy case"); the pre-answer check in an uplink round is a NAV check, not CCA; a slicing member keeps its own stream count (buildMuParts passes nss in both branches but records only ruFraction — surfaced when the reviewer's own derivation gave 92.8 vs the real 133.6 µs). ofdma-ul's numbers hit 618/550 with the procedure in: a paragraph was DELETED, not compressed, its claims living on in steps 4 and 6 (ruling: correct order of preference). C3 review dispatched (sonnet).
Housekeeping: C2 has left .tmp-c2-*.ts/.py probes in the worktree; remove at the gates.
C1 fix round (the AP ruling): 6b7e4e1 — txop and txop-protect say 接入点（AP）/"the access point (AP)" where the picture introduces it; edca and ampdu never named it on the main path, so they had no workaround to undo. Both budgets FELL (the bracket is shorter than the paraphrase). C1 review dispatched (sonnet).
B4 dispatched (opus): bianchi, bianchi-vs-sim, tier1-project, tier1-project-review — for the two model lessons the arithmetic IS the procedure and every symbol must be named where it appears; for the two project lessons the steps are the learner's, not the engine's.
C3 review: PASS (0 important, 2 minor). The reviewer re-derived all three engine findings from the code itself (respondToTrigger applies no power correction; the trigger-response gate is `now < navUntil && navSetBy !== from`, i.e. NAV; buildMuParts records nss only for MU-MIMO) and confirmed both earlier corrections still hold. Ruling: the minors are NOT carried to the track wave — minor 1 is stated-vs-simulated drift (the picture states the power correction as fact; the caveat sits in deeper, which a reader may never reach) and this programme's standing rule puts the qualification in the same breath as the claim. C3 fix round dispatched.
C1 review: PASS (0 important, 0 minor) — every step traced line by line to src/engine/mac.ts, the whole-aggregate decode claim confirmed at mac.ts:1416, the repaired AP seam reads naturally in both languages. Registered with C2's rule repair at 286384e.
RULE BUG 2 (found by C2): ten literal 0x08 bytes in tests/course/readability.test.ts regex literals — same class as ef74e20, older, from patch scripts that ate the escape. The Tier 2 English "router" ban and the English half of all four QUANTITIES rules had been grading vacuously. Repaired at 286384e; all lessons on the amendment stay green under the repaired rules, and C2's predicted AP conflict does not materialise (a090ebf already exempts 接入点（AP）).
C2: eb143fa DONE (width 1561, streams 1267, rate 1375, rate-fallback 1247). Engine over prose in `rate`: the near station sits at MCS 11 not because its signal runs out (58.7 dB would carry 13) but because mcsForPeer caps the ceiling when the pair has not negotiated 4096-QAM and rateScenario gives both ends only edca — pinned.
C3 fix round: f426c03 — the power-correction caveat moved into the picture in the same breath as the claim ("an instruction this simulator writes into the frame but does not act on"), and the dropped claim replaced by a measured one: 16,894 B per uploader = eleven whole frames + 531 B of padding, pinned over every TB PPDU of the run. The old loose claim ("fill to the byte, nothing wasted") is gone. C2+C3 review dispatched (sonnet).
C4: 877ec45 DONE (mlo 547/550 numbers, capstone 549/550). mlo closes on the engine's link-assignment procedure (one AcQueues per physical node shared by both link MACs, pokeAccess, independent per-link contention, the first link to zero claiming up to MAX_AMPDU_MPDUS off the shared queue, the sequence counter held with the queue, restore-to-front against SHORT_RETRY_LIMIT) with MSDU 66 walked through record by record. capstone's steps are the learner's method and its table is transposed so every row names its lane and counter. Honest carry: deeper's "a failure on one link can be retried on the other" is a code fact this scene never exercises — pinned at the queue level, not presented as something to watch. C4 review dispatched (sonnet).
U1 dispatched (opus): uwb-intro, uwb-frame, uwb-sts — first UWB batch; engine is src/uwb/, uwb-intro is the track opener (1000 words, 4 terms, no table), uwb-frame is the programme's reference pair so its register stays.
C2 + C3 review: PASS (0 important, 0 minor). Every procedure recomputed by hand against the engine; C2's capability-cap finding verified at simulation.ts:242 and wifiScenes.ts:122; ofdma-ul's rewritten padding figure verified against ampduPsduBytes/maxPsduBytesFor. Registered 40bf57d. 23 lessons on the amendment — every Wi-Fi lesson except B4's four.
Carry (Wi-Fi track fix wave): width's step 2 says "the receiver's noise figure" without glossing it — consider adding noise figure to QUANTITIES once the track is written rather than mid-flight.
U2 dispatched (opus): uwb-sstwr, uwb-dstwr, uwb-blocks — also asked to clear two of the four CELL_RULE_CARRIES (Treply1, SP1).
C4 review: NEEDS FIXES — 1 important (mlo step 6 teaches restore-to-front as the general rule; failMsdus drops at SHORT_RETRY_LIMIT=7 instead, and the scene produces no DROP records so the gap was unpinned — a procedure that is not the engine's, which is the defect this amendment exists to prevent), 1 minor (mlo's only "station"/站点 unglossed while every other lesson glosses it at first mention). The reviewer reproduced all five worked-example rows record for record and confirmed the cross-link caveat is correctly pinned at the queue level. Fix round dispatched: state both branches and pin the drop with a synthetic queue-level test, since the timeline will never show it.
C4 fix round: 672690c — mlo step 6 states both branches (drop with reason retryLimit at 7, restore-to-front below it), pinned on a synthetic copy of the scene with the laptop out of range because the lesson's own run yields no drop at all. Three new tests, and a bonus: the seven attempts alternate lanes, so deeper's cross-link retry claim now has timeline evidence too. Registered 4946d47 — Wi-Fi Tier 2 complete, 25 lessons on the amendment. Ruling recorded: a claim whose evidence cannot exist in the lesson's scene is pinned where its evidence does live (a synthetic scene or a unit-level test), never against a timeline that will never show it.
U3 dispatched (opus): uwb-position, uwb-geometry, uwb-coexist, uwb-contention.
B4: b380a79 DONE (bianchi 1497, bianchi-vs-sim 1505, tier1-project 1489, tier1-project-review 1474) — every Wi-Fi lesson is now written to the amendment. Engine over prose: bianchi's DISPLAYED formula is Bianchi's infinite-retry closed form while every number in the lesson, and bianchiModel.ts itself, uses the finite-retry chain at L=7. The batch kept the formula and added a clause to its note; the review is asked to judge whether a clause is enough or the displayed formula must become the one the code runs. All four sit within three words of the numbers ceiling — the next addition to any of them forces a split.
B4 review dispatched (sonnet), weighted onto the formula question.
U1: d7b8876 DONE (uwb-intro 999/1000 opener, uwb-frame 1381, uwb-sts 1380). Procedures from src/uwb/ in its order (SS-TWR through transmitFor/onRx/replyRctu/onResponse/ssTwrRaw/rctuToMetres; uwbPpduLayout + the 73.269 us counter read + 18.1 dB SYNC accumulation + first chip after SFD + rounding to 15.650 ps ticks; the attacker branch vs stsOff). The simulator generates no STS — it is a switch plus a fixed-advance attacker — and the lesson says so. U1 review dispatched (sonnet).
B4 review: NEEDS FIXES — 1 important. The displayed formula is the infinite-retry closed form; the table three lines below is captioned "what the equations predict" and lists finite-retry numbers (48.09 % vs 49.59 % at n=20, both pinned, both right for their own model). RULING: display the equations the numbers come from — the finite-retry pair the code solves — and move the classic closed form to `deeper`, labelled, with the difference it makes as the illustration. No pinned number changes; the formula on the page was the wrong one, not the figures. Fix round dispatched.
U1 review: NEEDS FIXES — 1 important (uwb-sts's picture and steps narrate a live correlation — "looks for the pulses it generated itself… finds noise there" — while onRxOk (device.ts ~491-503) is a boolean gate on the scenario's attacker config: nothing is generated, nothing compared, and on an honest reception no check runs at all; the disclosure sat in `deeper`). Every other step and figure re-derived exactly (73.269 us, 18.1 dB, 15.650 ps, 3195 ticks, 14.99 m, the 32 768 vs 32 512 chip correction). RULING: separate the two voices on the main path — what real hardware and the standard do, attributed as such, and what this simulator does (the switch), in the same breath. The simulator models the OUTCOME of the check, not the check; that is why the attacker's advance is a fixed 50 ns. Fix round dispatched.
B4 fix round: 4acf9a7 — bianchi displays the finite-retry pair solveBianchi solves, with L, W_i, W and m named where they appear; the caption says "What these two equations predict"; the classic closed form moved to `deeper`, labelled, carrying 48.09 % vs 49.59 % at n=20 as the size of the difference. No quoted number changed. Registered 8a8a74d — ALL 29 WI-FI LESSONS ARE ON THE AMENDMENT.
Wi-Fi whole-track review dispatched (opus, c395e14..HEAD, Wi-Fi files): cross-batch procedure drift, stated-vs-simulated survivors, the 29-lesson reading path, ZH as ZH, and whether the rules themselves can still fail.
U1 fix round: b6cb2f0 — uwb-sts separates the two voices on the main path (a new picture paragraph "What you are actually watching" + step 3: no key, no pulses, nothing compared; the simulator models the OUTCOME, which is why the advance is a fixed 50 ns). Two pins re-aimed at onRxOk, including one asserting no main-path string still carries the old correlation sentences. Registered 732231e.
U2: 0054ab7 DONE (uwb-sstwr 1570, uwb-dstwr 1537, uwb-blocks 1477). Cleared BOTH of its CELL_RULE_CARRIES plus a third defect the carry was hiding, and verified by deleting the entries, re-running green and restoring the controller's file byte for byte.
RULING on the 600 RSTU correction — U2 challenged it with code rather than complying, and is right. ScenarioSchema floors EVERY session at 300 RSTU (scenario.ts:595); the `% 300` rule and the fragment-fit rule that make 600 the shortest usable slot sit inside the MMS branch (scenario.ts:692-708). So: 600 is the MMS floor, not the general one. uwb-blocks' 300 RSTU / 250.0 us for a 4z DS-TWR session is correct and pinned against schemaIssues. I checked every "600 RSTU" claim in the course — all already MMS-scoped, nothing else to correct. The earlier ledger entry was over-broad; this is the accurate statement.
Registered de1521f. CELL_RULE_CARRIES is down to uwb-ul-tdoa and uwb-mms-numbers.
U4 dispatched (opus): uwb-dl-tdoa, uwb-ul-tdoa, uwb-aoa — and asked to clear the uwb-ul-tdoa carry.
U3: 89f152f DONE (uwb-position 1383, uwb-geometry 1439, uwb-coexist 1520, uwb-contention 1560). Engine over prose: solvePosition returns Fix.residualM but NO record carries it — fmtRecord never prints it and the inspector never shows it — so "you can see it without knowing which range lied" and "nothing else on screen moves" were both implying a figure the reader cannot see; corrected to "the solver knows that" / "Inside the solver … no record carries that figure", with the residuals pinned through solvePosition directly. CARRY (engine gap, for the user to decide): a reader cannot read a residual off the screen at all. U3 independently confirmed the slot-floor scope. Two supporting paragraphs moved numbers→deeper rather than compressed; the review is checking nothing load-bearing went with them.
U2+U3 review dispatched (sonnet). U5 dispatched (opus): the four 802.15.4ab lessons, incl. clearing the uwb-mms-numbers carry and the 这笔账 in uwb-nba-coexist.
WI-FI WHOLE-TRACK REVIEW: NEEDS FIXES — 7 important, 11 minor. Verdict: "the track does now teach its mechanisms … a fix wave short of merging, not a rewrite short." The three worst: (1) edca's worked example loses 9 us — the row its own step 5 states (a boundary with the counter already at zero transmits a further slot later, mac.ts:486-491) is missing, so a reader redoing it lands on 23.1246 not 23.1336; (2) frame-anatomy, ifs and nav narrate an FCS check the engine NEVER runs — there is no MPDU CRC in src/engine/ at all; channel.ts:698-705 decides on worst SINR vs the rung's requirement (edca and bianchi-vs-sim already say "could not decode", which is how the drift shows); (3) RULE 2 could not fail for 27 of 29 lessons. Also: mumimo sends the reader to watch a sounding exchange the simulator does not run; airtime states a fixed 24 Mb/s ACK rate where the engine picks the highest mandatory rate <= the frame's reference rate (phy.ts:137-144); the preamble has three names across the track.
Controller fixed a9a253c: rule 2's pool is now the lesson's own terms + the transitive closure of its `needs` (was every earlier lesson in COURSE_ORDER — decode-thresholds alone seeded all four quantities); the blocklist now catches a quantity behind the idiom ("dB in hand", 手里仍留) while leaving ordinary English alone; decode-thresholds' own observe item — the lesson this amendment was written for — said "keeps 3 dB in hand" and now names it as rate margin.
U2+U3 review: PASS (0 important, 1 minor). Registered 749743d — 38 lessons on the amendment.
Wi-Fi fix wave dispatched (opus): the whole review minus the three findings the controller already closed.
U4: db6e645 DONE (uwb-dl-tdoa 1481, uwb-ul-tdoa 1547, uwb-aoa 1566). Cleared the uwb-ul-tdoa cell carry with a regression test in the lesson's own file; dropped from the list at 8c7c870, leaving only uwb-mms-numbers. CONTROLLER ERROR caught by the batch: my dispatch named a `UWB_FIX` record; there is no such record — it is `UWB_POSITION` (src/uwb/records.ts:48). The lessons and pins use the real name. U4 review dispatched (sonnet), weighted onto which clock matters in each TDoA direction and onto uwb-aoa's honesty (the simulator never runs two receivers — it computes the phase a half-wavelength pair would see, adds one noise draw, and inverts it).
U4 review: PASS (0 important, 0 minor). The mirrored-pair risk was checked directly and is clean: downlink divides out the badge's crystal through the Poll-to-Final ratio, uplink applies no rate correction because the anchors share a timebase. uwb-aoa's disclosure sits in the picture right after the opening paragraph. Registered bbe2882 — 41 lessons on the amendment; only U5's four remain.
U5: 2396f99 DONE (uwb-mms 1506, uwb-mms-numbers 1490, uwb-nba 1498, uwb-nba-coexist 1536); review PASS (0/0). Engine correction: UWB_PL_EXP is 2.0 (free space, including the narrowband radio) while the Wi-Fi indoor exponent 3.0 belongs to propagation.ts — the lesson keeps them apart. Last cell carry cleared; CELL_RULE_CARRIES = {}. Registered a83b2d5.
CONTROLLER ERROR: uwb-capstone was never dispatched — the plan lists U6 and I never sent it. Found by registering it and watching all three rules fail, not by reading the plan. U6 dispatched now (opus). 45 of 46 non-AMP lessons on the amendment.
Spec amendment gained three authoring notes (ea62465): steps carry words and a table beside them carries figures; a heading counts as a first use; a claim whose evidence cannot exist in the scene is pinned where its evidence lives.
WI-FI FIX WAVE: 128bae5 — all six in-scope importants fixed (edca's closing arithmetic + the closure pinned; the FCS check removed from frame-anatomy/ifs/nav in both languages, pinned with "no engine file computes a CRC" and RX_FAIL-reason tests; mumimo's sounding disclosure on the main path; airtime's control-response rate against ctrlRespRateFor with nav/bianchi/tier1-project pointing back; one name for the preamble across nine lessons) plus 8 minors. tests/course 2448 passed.
CONTROLLER ERROR, caught by the fix wave: commit a9a253c CLAIMED the rule-2 narrowing but only the blocklist half landed — my patch script asserted on a later hunk and exited before writing, and I wrote the message as though both had. The rule stayed vacuous for another dozen commits. Landed properly at db2ff0e; with it in force it fails six lessons (roles-stack fixed there, five UWB to the fix wave). Second time in this programme a rule was reported fixed while grading nothing; first time the false report was mine.
Deferred to the controller by the fix wave, correctly: M1 (the ZH arm of the naming rule grades only Latin acronyms) and M9 (the AP/STA bracket is ritual by Tier 2) both live in controller files shared with the running UWB work.
U6: db9fd25 DONE — uwb-capstone as the learner's method; the rubric's "report the residual" removed (no record carries it; a test asserts the word appears nowhere on the main path) and a step corrected (anchor-1's five errors are all slightly negative, so "wander either side of zero" was false). Registered d1662a7. ALL 46 NON-AMP LESSONS ARE ON THE AMENDMENT.
UWB whole-track review dispatched (opus), with the five rule-2 failures and the ZH bullet handed to it as known context.
UWB WHOLE-TRACK REVIEW: NEEDS FIXES — 8 important, 13 minor. The worst is a physics error propagated across five places: the MMS fragment train is spaced (R+1) x slot = 2 ms in the base one-to-many scene, NOT 1 ms (session.ts:105, device.mms.ts:145/463; mms.ts:131-133 says the true millisecond holds only in the pairwise round). uwb-mms' step 3 and its 4/ms cell, uwb-mms-numbers' "exactly a millisecond apart" + its ratio formula + "7 ms, sigma 0.0202 ppm" (really 14 ms and 0.0101 ppm), and uwb-capstone's "a fragment from every device in every millisecond" are all wrong — and uwb-mms' own title and observe line contradict them. The pin was vacuous: the test hard-codes ratioSigma(tsNoisePs, 7) instead of deriving the span. Also: uwb-capstone denies the redundancy uwb-position teaches and miscounts the unknowns (two, position.ts:87-116); uwb-position still promises a residual no record carries.
RULE BUGS 4 and 5, both found by that review and repaired at d9092ec (branch deliberately red, 17 failures, every one real):
 - rule 2 pooled the TRANSITIVE closure of needs, and uwb-intro -> frame-anatomy -> roles-stack -> decode-thresholds, so every UWB lesson inherited all four quantities from the lesson that made the rule vacuous in the first place. Pool is now the lesson + its DIRECT needs. 12 failures, seven of them Wi-Fi lessons the track review and its fix wave had both already been through.
 - the naming rule's Chinese arm graded only Latin tokens: namedInPlace returns true for a term it cannot find, and a Term's name is one string shared by both languages. A CJK name is now matched literally (a word boundary means nothing between ideographs) and zhTermName reads the Chinese name a gloss opens with. 5 failures.
Three rules in this suite have now graded nothing while reporting success. Two fix waves dispatched: UWB (the review + 7 rule failures) and Wi-Fi (8 rule failures).
WI-FI RULE-2 WAVE: 01531de — all eight lessons green, none split. Notable calls: ifs/backoff/streams reworded rather than given a term (the quantity was already named and sized in the same clause, so the word was redundant); backoff's ACK-timeout cell now says "an answer that begins late", which is strictly more informative than "margin"; tier1-project RE-ADDED the margin term, deliberately reversing the Wi-Fi track review's M2 deletion — that deletion rested on both owners being in `needs`, which was true only transitively and the repaired rule no longer accepts. The implementer refused to bolt decode-thresholds onto tier1-project's needs for one word, which is the right call: a dishonest prerequisite to satisfy a rule is the rule defeating itself. RULING: accepted, and the reversal is recorded in the lesson's own test comment.
RULE REFINEMENT f8bf796: 余量 matched inside 剩余量 and made nav reword a sentence to escape a rule that should not have fired — the wrong direction of fix. The rule now excludes 剩/存/养 before 余量. Every Wi-Fi lesson is green under both repaired rules; the five remaining failures are UWB and belong to the wave in flight.
UWB FIX WAVE: 7928b59 — all 8 importants and 11 of 13 minors. I1 corrected TWELVE instances of the 1 ms fragment gap (three more than the review found: a deeper paragraph, a quiz option and its explain, and the file docstring), and the test now DERIVES the span from roundPlan instead of hard-coding 7, asserting the pairwise round's true millisecond beside it. Side effect worth recording: glossing `threshold` made it uwb-coexist's word under the acronym rule, which broke uwb-mms/uwb-mms-numbers; rather than bolt uwb-coexist onto the MMS needs (a false promise), those three sentences now say *sensitivity*, which uwb-mms owns — which is what they meant. Not fixed, with reasons: M2's scene-naming pass (phone/tag/badge across 18 lessons — a rewrite in its own right for a Minor, and it needs the controller to settle the three scenes' names) and M11/M13, controller files.
Controller c8d14b7: the engine's OWN comments said "a millisecond" in three places — mms.ts' docstring, its rmarkerFromFragment doc and device.mms.ts beside the ratio — three lines above the gapRctu comment that says the true millisecond holds only in the pairwise round. All corrected; MS_NS keeps its name and value and now says which round it is the spacing of. The empty CELL_RULE_CARRIES keeps its comment, rewritten: every id it once excused was fixed, not re-excused.
GATES on a quiet tree: tree clean, tsc clean, 159 files / 3691 tests green, build green, fixtures byte-identical with main (no scenario touched anywhere in the programme). Merging.
CARRIES for a later pass: the scene-naming pass (M2); four UWB numbers sections at their ceiling (uwb-mms 548, uwb-mms-numbers 550, uwb-blocks 550, uwb-coexist 548) and five Wi-Fi ones, so the next correction in any of them forces a split; `width` says "noise figure" unglossed and QUANTITIES could grow to cover it; the AP/STA bracket has become ritual in Tier 2; and the engine gap the reader would most benefit from — solvePosition computes a residual that no record carries and no inspector row prints, which is the one number that would let a learner see their anchor layout is bad.
